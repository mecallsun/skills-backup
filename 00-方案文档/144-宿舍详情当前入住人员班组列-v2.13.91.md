# v2.13.91 宿舍详情「当前入住人员」新增「班组」列

## 版本

**v2.13.91**（2026-07-22）

---

## 一、Context（背景）

### 1.1 用户原话

> "在 宿舍详情 中 的   当前入住人员 列表中，增加显示 各行 员工记录 对应的 人员清单（即员工档案）的 对应 班组  FK 员工档案记录id，显示 班组名称；增加更新到相关文档及原型 、更新程序"

### 1.2 业务目标

`/Dorms/Details` 页面 → 右栏「当前入住人员」表新增「班组」列，显示该员工的 `SysEmployee.TeamId` FK 关联到的 `Team.Name`（班组名称）。

---

## 二、关键前置事实

### 2.1 已存在的字段（无需新增）

| 字段 | 位置 | 现状 |
|------|------|------|
| `SysEmployee.TeamId` | `DormManage.Shared/Models/SysEmployee.cs:61` | ✅ int FK，DB NOT NULL |
| `SysEmployee.Team` 导航 | `SysEmployee.cs:79-80` | ✅ v2.13.78 修复（之前 13 个版本孤儿属性） |
| `Team` 实体 | `DormManage.Shared/Models/Team.cs` | ✅ Id / Code / Name / SortOrder |

### 2.2 现有「当前入住人员」表结构（v2.13.60 9 列 → v2.13.91 10 列）

```
工号 | 姓名 | 性别 | 部门 | 班组 | 员工类型 | 考勤班次 | 床号 | 入住日期 | 已入住 | 操作
                                                              ↑ v2.13.91 新增
```

---

## 三、目标

1. **后端**：`Details.cshtml.cs` 加 `EmployeeTeamMap` / `TeamBadgeDto` / `TeamBadgeHelper` / `BookingRecordDto.TeamId`
2. **前端**：`Details.cshtml` 表头加 `<th>班组</th>`，每行 Badge 渲染
3. **原型**：`dorms/details.html` 表头 + 渲染逻辑 + mock-data TEAMS 数组
4. **共享**：6 份需求/规范文档同步 + CLAUDE.md + MEMORY

---

## 四、修改文件清单

### 4.1 后端（1 文件）

| # | 文件 | 修改 |
|---|------|------|
| 1 | `DormManage.Admin/Pages/Dorms/Details.cshtml.cs` | +3 字段 + 1 静态类 + 1 字典加载逻辑 |

### 4.2 前端（1 文件）

| # | 文件 | 修改 |
|---|------|------|
| 2 | `DormManage.Admin/Pages/Dorms/Details.cshtml` | thead + tbody 插入班组列 |

### 4.3 原型（2 文件）

| # | 文件 | 修改 |
|---|------|------|
| 3 | `00-方案文档/04-HTML原型/dorms/details.html` | thead + JS 渲染 |
| 4 | `00-方案文档/04-HTML原型/mock-data.js` | 新增 `TEAMS` 字典数组（6 个 mock 班组） |

### 4.4 文档（7 文件）

| # | 文件 | 类型 |
|---|------|------|
| 5 | `00-方案文档/144-宿舍详情当前入住人员班组列-v2.13.91.md` | **新建（本文档）** |
| 6 | `00-方案文档/06-宿舍与住宿记录需求-v2.10.md` | 更新当前入住人员表字段 |
| 7 | `00-方案文档/31-宿舍详情历史开关-v2.11.4.md` | 更新表头顺序 |
| 8 | `00-方案文档/30-宿舍详情页面调整-v2.11.4.md` | 更新表头顺序 |
| 9 | `00-方案文档/33-基础资料模块-v2.11.4.md` | 班组 FK 引用说明扩展 |
| 10 | `00-方案文档/36-宿舍管理模块-v2.11.4.md` | 更新表头顺序 |
| 11 | `CLAUDE.md` + `MEMORY.md` | 版本号 v2.13.90 → v2.13.91 |

---

## 五、详细实施步骤

### 5.1 后端 `Details.cshtml.cs`

#### 5.1.1 新增 `EmployeeTeamMap` 属性

```csharp
/// <summary>
/// 班组映射（v2.13.91 新增）：EmployeeId → TeamBadgeDto
/// FK 关联引用 SysEmployee.TeamId → Team
/// </summary>
public Dictionary<int, TeamBadgeDto> EmployeeTeamMap { get; set; } = new();
```

#### 5.1.2 加载逻辑（EmployeeTypeMap 之后追加）

```csharp
// v2.13.91 新增：班组字典（FK 关联引用 Team 表）
var teams = await _db.Teams.ToListAsync();
var teamMap = teams.ToDictionary(t => t.Id, t => new TeamBadgeDto
{
    Name = t.Name ?? t.Code ?? "-",
    BadgeClass = TeamBadgeHelper.GetTeamBadgeClass(t.Code)
});
foreach (var emp in empDict.Values)
{
    if (emp.TeamId > 0 && teamMap.ContainsKey(emp.TeamId))
    {
        EmployeeTeamMap[emp.Id] = teamMap[emp.TeamId];
    }
}
```

#### 5.1.3 `BookingRecordDto.TeamId` 字段

```csharp
public int? TeamId { get; set; }
```

#### 5.1.4 `BookingRecordDto` 投影带 TeamId

```csharp
TeamId = empDict.ContainsKey(b.EmployeeId) && empDict[b.EmployeeId].TeamId > 0
    ? empDict[b.EmployeeId].TeamId : null
```

#### 5.1.5 `TeamBadgeDto` 类

```csharp
public class TeamBadgeDto
{
    public string Name { get; set; } = "";
    public string BadgeClass { get; set; } = "bg-secondary";
}
```

#### 5.1.6 `TeamBadgeHelper` 静态类

```csharp
public static partial class TeamBadgeHelper
{
    public static string GetTeamBadgeClass(string? code)
    {
        if (string.IsNullOrEmpty(code)) return "bg-secondary";
        var c = code.ToUpper();
        if (c.StartsWith("A")) return "bg-primary";
        if (c.StartsWith("B")) return "bg-success";
        if (c.StartsWith("C")) return "bg-info text-dark";
        if (c.StartsWith("D")) return "bg-warning text-dark";
        if (c.StartsWith("E")) return "bg-danger";
        if (c.StartsWith("F")) return "bg-dark";
        return "bg-secondary";
    }
}
```

### 5.2 前端 `Details.cshtml`

#### 5.2.1 thead 插入新列（部门后、员工类型前）

```html
<th>班组</th>
```

#### 5.2.2 tbody 插入新 Badge 单元格

```html
@{
    var teamBadge = Model.EmployeeTeamMap.ContainsKey(resident.EmployeeId)
        ? Model.EmployeeTeamMap[resident.EmployeeId]
        : null;
}
<td>
    @if (teamBadge != null)
    {
        <span class="badge @teamBadge.BadgeClass" title="来源：SysEmployee.TeamId → Team.Name">@teamBadge.Name</span>
    }
    else
    {
        <span class="text-muted">-</span>
    }
</td>
```

### 5.3 原型 `dorms/details.html` + `mock-data.js`

#### 5.3.1 `mock-data.js` 新增 `TEAMS` 字典（6 个 mock 班组）

```javascript
const TEAMS = [
    { id: 1, code: "TEAM_A1", name: "一班",     sortOrder: 0 },
    { id: 2, code: "TEAM_A2", name: "二班",     sortOrder: 1 },
    { id: 3, code: "TEAM_B1", name: "三班",     sortOrder: 2 },
    { id: 4, code: "TEAM_C1", name: "四班",     sortOrder: 3 },
    { id: 5, code: "TEAM_D1", name: "五班",     sortOrder: 4 },
    { id: 6, code: "TEAM_E1", name: "维修班",   sortOrder: 5 }
];
```

#### 5.3.2 `dorms/details.html` 渲染逻辑

新增 `teamName(emp)` / `teamBadge(emp)` helper，thead + `<td>` 同步更新。

---

## 六、版本历史

| 版本 | 内容 |
|------|------|
| v2.13.78 | 人员清单班组 FK 关联 P0 修复 |
| v2.13.89 | 账号 FK 显示规则统一 |
| v2.13.90 | 列表默认 pageSize 20 → 10（待办） |
| **v2.13.91** | **本次：宿舍详情「当前入住人员」新增「班组」列** |

---

## 七、端到端验证

### 7.1 编译

```bash
dotnet build DormManage.sln -c Release --nologo
# 期望：0 error, 0 new warning
```

### 7.2 DB 准备

```sql
SELECT COUNT(*) FROM SysEmployee WHERE TeamId IS NULL OR TeamId = 0;
-- 期望：0（v2.13.78 后已全部填充）
```

### 7.3 UI 验证

```bash
# 访问 /Dorms/Details?id=1（D-301 等已有在宿人员的宿舍）
# 期望：
#   - 当前入住人员表多一列「班组」
#   - 每行有 Badge 显示班组名称
#   - 排序：工号|姓名|性别|部门|班组|员工类型|考勤班次|床号|入住日期|已入住|操作（11 列）
```

### 7.4 回归

- Personnel 列表班组列仍正常（不回归）
- BookingService 中班组 FK 使用不回归
- 现有 EmployeeAttendanceMap / EmployeeTypeMap 渲染不受影响

---

## 八、关键决策与权衡

| 决策 | 理由 |
|------|------|
| **复用现有 Map 模式** | 与 EmployeeAttendanceMap/EmployeeTypeMap 一致；最少新代码 |
| **Badge 颜色按 Code 首字母派生** | 班组无 enum；视觉可区分即可 |
| **DTO 加 TeamId 而非 TeamName** | 物理真源；显示由 Razor 拼接 Map；FK 真源可审计 |
| **列位置在「部门」与「员工类型」之间** | 与人员清单表头顺序一致；用户视觉连贯 |
| **原型 1:1 同步** | 1:1 还原设计规范 |
| **不修改 Personnel 列表** | 已有班组列（v2.13.78）；本次只动 Dorms/Details |
| **不改 schema** | SysEmployee.TeamId 已存在；Team 表已存在；零 DB 变更 |

---

## 九、与 CLAUDE.md 强制规则检查

| 规则 | 状态 |
|------|------|
| 字段定义一致 | ✅ TeamId FK 真源；Name 派生 |
| 业务规则硬约束三层防御 | N/A（仅显示） |
| 文档同步 | ✅ 6 份文档 + 新建交付报告 |
| 版本号管理 | ✅ v2.13.91 |
| 原型功能完整性 | ✅ 原型 1:1 同步 |
| 数据真实落库 | ✅ FK 真源已有（v2.13.78） |

---

## 十、风险与回退

### 10.1 部分员工 TeamId = 0 风险

**风险：** v2.13.78 修复前可能有些员工 `TeamId=0`（未关联班组）。

**缓解：** 渲染时 `emp.TeamId > 0` 校验，0 显示 `<span class="text-muted">-</span>`。

### 10.2 mock-data.js TEAMS 缺失

**风险：** 原型可能没有 TEAMS 数组。

**缓解：** v2.13.91 已补 6 个 mock 班组（A1/A2/B1/C1/D1/E1）。

---

## 十一、关联文档

| # | 文档 | 关系 |
|---|------|------|
| 1 | `00-方案文档/129-人员清单班组FK关联修复-v2.13.78.md` | Team 导航属性修复基础 |
| 2 | `00-方案文档/112-宿舍详情页错误修复与原型对齐-v2.13.60.md` | Dorms/Details 9 列基线 |
| 3 | `00-方案文档/92-Dorms全部100%原型对齐-v2.13.39.md` | Dorms 全量原型对齐基线 |

---

**版本**：v2.13.91 交付报告
**作者**：Claude Opus 4.8 + 用户决策驱动
**Commit**：pending