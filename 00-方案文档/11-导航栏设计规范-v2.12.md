# 导航栏设计规范 v2.12（已升级）

> **版本**：v2.12.0
> **日期**：2026-07-12
> **状态**：**已升级 / 历史归档**
> **适用版本**：v2.11.0 ~ v2.11.6（保留兼容）
> **新规范**：自 v2.12.0 起，请遵循 [`37-共用页头与Tab页签导航设计规范-v2.12.md`](./37-共用页头与Tab页签导航设计规范-v2.12.md)

---

## ⚠️ 重要变更通知

本规范自 **v2.12.0** 起被新规范 **37-共用页头与Tab页签导航设计规范** 替代。本文档保留 v2.11 系列的双层导航设计内容，作为**历史版本**供查阅；新增 / 修改需求请直接参照新规范。

### 主要变更对照

| # | 旧模式（v2.11） | 新模式（v2.12） | 文档位置 |
|---|----------------|----------------|---------|
| 1 | 顶部品牌栏 + 单层菜单导航 | 顶部品牌栏 + 紧凑型图标导航条 + Tab 页签栏 | 37 §2 |
| 2 | 点击菜单整页跳转 | 点击图标新增 Tab，原 Tab 保留 | 37 §4.5 |
| 3 | 浏览器 history 管理 | localStorage 持久化 Tab 列表 | 37 §5.5 |
| 4 | 菜单项图标+文字横向排列 | 仅图标 + 文字 Tooltip，紧凑布局 | 37 §4 |
| 5 | 无 Tab 概念 | 多 Tab 可关闭、可固定、可拖拽 | 37 §5 |

> **迁移建议**：现有 v2.11.6 项目可直接升级到 v2.12，详见 37 §10 验收清单。

---

## 1. 历史版本：v2.11 整体架构（保留查阅）

采用 **双层导航** 布局（v2.11 时代）：

```
┌─────────────────────────────────────────────────────────┐
│  Tier 1: 顶部品牌栏 (top-bar)                             │
│  [Logo + 品牌名]                    [用户信息 + 退出]     │
├─────────────────────────────────────────────────────────┤
│  Tier 2: 菜单导航栏 (menu-bar)                            │
│  [首页][记录] | [住宿] [人员] | [费用标准][费用清单][分摊] │
│  | [旧员工][用户][角色] | [系统设置]                      │
└─────────────────────────────────────────────────────────┘
```

- **Tier 1** 固定顶部 (`sticky top:0`)，包含品牌标识和用户操作
- **Tier 2** 紧随其下，包含所有功能菜单，横向滚动自适应
- 两层之间有 `1px` 分隔线，整体视觉层次清晰

---

## 2. 历史版本：v2.11 色彩体系（保留查阅）

> 注：v2.12 新规范沿用同一组类别色，详见 `37 §4.3`。

| 类别 | 色值 | 用途 |
|------|------|------|
| 核心功能 (蓝色) | `#1976d2` | 首页、记录、住宿、办理登记 |
| 人员 (绿色) | `#2e7d32` | 人员清单 |
| 费用 (橙色) | `#e65100` | 费用标准、住宿费用清单、员工分摊费用 |
| 抄表 (青色) | `#00838f` | 抄表记录 |
| 管理 (紫色) | `#6a1b9a` | 旧员工、用户、角色 |
| 设置 (灰色) | `#546e7a` | 系统设置 |
| 默认文字 | `#5f6368` | 非激活菜单项 |
| 背景 | `#ffffff` | 导航栏底色 |
| 分隔线 | `#e8eaed` | 菜单分隔符 |

> 色板经过 CVD 安全校验，相邻色块 ΔE ≥ 12，符合 WCAG AA 对比度标准。

---

## 3. 历史版本：v2.11 视觉规范（保留查阅）

### 3.1 顶部品牌栏 (top-bar)

| 属性 | 值 |
|------|---|
| 高度 | 自动 (约 48px) |
| 背景 | `#ffffff` |
| 底边框 | `1px solid #e8eaed` |
| 阴影 | `0 1px 4px rgba(0,0,0,0.04)` |
| 品牌图标 | `bi bi-droplet-half`，1.5rem，蓝色渐变 |
| 品牌文字 | 1.2rem，加粗，`#1976d2` |
| 版本号 | 0.7rem，灰色 `#9e9e9e`，左侧竖线分隔 |
| 用户胶囊 | 圆角 24px，背景 `#f5f7fa`，头像图标蓝色 |
| 退出按钮 | 圆角 20px，边框 `#e0e0e0`，hover 变红色 |

### 3.2 菜单导航栏 (menu-bar)

| 属性 | 值 |
|------|---|
| 背景 | `#ffffff` |
| 底边框 | `1px solid #e8eaed` |
| 阴影 | `0 2px 6px rgba(0,0,0,0.03)` |
| 菜单项内边距 | `0.85rem 1.15rem` |
| 字体大小 | `0.9rem` |
| 图标大小 | `1.15rem` |
| 激活下划线 | `3px solid` 类别色 |
| 激活背景 | `rgba(类别色, 0.06)` |
| Hover 背景 | `rgba(25, 118, 210, 0.04)` |
| 过渡动画 | `cubic-bezier(0.4, 0, 0.2, 1) 0.2s` |
| 图标 hover 缩放 | `scale(1.12)` |

### 3.3 分隔符

| 属性 | 值 |
|------|---|
| 宽度 | `1px` |
| 颜色 | `#e8eaed` |
| 上下边距 | `0.5rem` |

---

## 4. 历史版本：v2.11 交互行为（保留查阅）

### 4.1 菜单项状态

| 状态 | 表现 |
|------|------|
| 默认 | 灰色文字 `#5f6368`，无边框 |
| Hover | 背景淡蓝 `rgba(25,118,210,0.04)`，图标放大 1.12x |
| 激活 | 类别色文字 + `3px` 底部色条 + 淡色背景 |

### 4.2 退出按钮

| 状态 | 表现 |
|------|------|
| 默认 | 灰色边框 `#e0e0e0`，灰色文字 |
| Hover | 红色边框 `#e53935`，红色文字，淡红背景 |

### 4.3 自动高亮

JavaScript 根据当前 URL pathname 自动匹配菜单项并添加 `active` 类：

```javascript
var currentPath = window.location.pathname.replace(/\/$/, '');
document.querySelectorAll('.menu-item').forEach(function(link) {
    var href = link.getAttribute('href');
    if (href && (currentPath === href || currentPath.startsWith(href))) {
        link.classList.add('active');
    }
});
```

---

## 5. 历史版本：v2.11 响应式处理（保留查阅）

- 菜单栏支持横向滚动 (`overflow-x: auto`)
- 隐藏滚动条 (`scrollbar-width: none`)
- 菜单项 `white-space: nowrap` 防止换行
- 移动端可折叠为汉堡菜单（预留扩展）

---

## 6. 历史文件清单（v2.11 时代）

| 文件 | 说明 |
|------|------|
| `05-Standalone/Admin/Views/Shared/_Layout.cshtml` | Razor 主布局模板 |
| `00-方案文档/04-HTML原型/mock-data.js` | 原型子页面导航渲染函数 |
| `00-方案文档/04-HTML原型/index.html` | 原型首页 |
| `09-原型Web部署/www/mock-data.js` | Web部署版原型子页面导航 |
| `09-原型Web部署/www/index.html` | Web部署版原型首页 |

> ⚠️ 上列文件从 v2.12.0 起将被 `DormManage.Admin/Pages/Shared/_Layout.cshtml` + `04-HTML原型/_shared/layout.html` 取代。

---

## 7. 与设计系统的对齐（历史保留）

本导航设计遵循 dataviz 技能的设计原则：

- **色彩职责固定**：每个类别使用固定色，过滤/排序不改变颜色分配
- **单一坐标轴**：无双重编码，颜色仅表示类别身份
- **文字使用文字色**：数值/标签使用中性墨色，彩色图标承载身份
- **Thin marks**：菜单项边框仅 3px 底部色条，不喧宾夺主
- **无障碍**：颜色 + 图标 + 文字三重编码，不依赖单一颜色传达信息

---

## 8. 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v2.11.0 | 2026-07-08 | 初始版本（双层导航：品牌栏 + 菜单栏） |
| v2.11.1 | 2026-07-09 | 调整色彩对比度 + 增加 CVD 校验说明 |
| v2.11.2 | 2026-07-10 | 适配首页数据看板 v2.11.2 |
| v2.12.0 | 2026-07-12 | **整体升级**：被 [`37-共用页头与Tab页签导航设计规范`](./37-共用页头与Tab页签导航设计规范-v2.12.md) 替代；本文档降级为历史归档 |