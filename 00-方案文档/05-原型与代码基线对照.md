# 原型-代码基线对照报告 — v2.13.24

> **目的**：验证 HTML 原型与已实现的 Razor 视图在字段、跳转、按钮动作上的一致性  
> **日期**：2026-07-19  
> **变更说明**：统一 7 个列表页筛选区布局与风格（宿舍档案/智能抄表/办理登记/人员管理/费用标准/宿舍账单/员工账单），全面应用 `filter-card`/`filter-row flex-nowrap`/`filter-btn 90×38px` 规范；房号/宿舍号统一改为 `<input list>` combobox；重置按钮统一接入 `FilterPersistence.resetFilters('{module}')`；`EmployeeBilling` 补齐部门/员工类型/住宿状态筛选；`BillingStandard` 补齐适用类型筛选；页内重复 CSS 全部清理并合并至 `_Layout.cshtml` 全局样式。  
> **结论**：✅ 除测试文件外，所有 26 个 HTML 原型页面均有对应的 Razor 实现（v2.13.86 新增 personnel/details.html）

> **v2.13.86 更新**：
> - **新增** `personnel/details.html` 原型（独立详情页，左右两栏布局：基本信息 + 当前住宿 + 历史记录）
> - **6 个原型补充性别字段**（与代码 v2.13.83/86 1:1 对齐）：
>   - `personnel/list.html`：列表新增「性别」列（14→15 列，colspan 同步）
>   - `personnel/create.html` + `edit.html`：基本信息区块新增「性别 select」（必填）
>   - `personnel/import.html`：模板列 11→12 列（含「性别」第 3 列）
>   - `booking/index.html`：列表新增「性别」列（12→13 列）
>   - `booking/check-in.html` + `check-out.html`：emp-info-card 新增「性别」字段
>   - `booking/edit.html`：员工信息块右侧追加性别 Badge
>   - `billing/employee-bills.html`：姓名后追加性别 Badge（16→17 列）
>   - `dorms/details.html`：入住人员表新增「性别」列（9→10 列）
> - 所有性别 Badge 统一规范：男=primary蓝 / 女=danger粉 / 未知=secondary灰（含 Bootstrap Icons `bi-gender-male/female`）

> **v2.13.21 更新**：宿舍详情 `Dorms/Details.cshtml` 基本信息区域显示风格与原型对齐，标签与数据强制同一行显示；所有列表页底部页码统一为标准 3 按钮分页（上一页 / 当前前后 2 页 / 下一页），`DormBilling/Index.cshtml` 与 `BillingStandard/Index.cshtml` 的 5 按钮分页已回退为 3 按钮。

> **v2.13.24 更新**：修正 §1.1 标题"20 个" → "25 个"（与表格行数对齐）；删除第 17/25 行 `dorms/details.html` 重复条目；修正结论"26 个" → "25 个"。

> **v2.13.32 / v2.13.33 更新**：
> - 办理登记 `Booking/Index.cshtml` 重构为 PageHeader + 列表 + 入住/退房 modal（`checkInModal`/`checkOutModal`），主路径走弹窗；`Booking/CheckIn.cshtml` 与 `Booking/CheckOut.cshtml` 仍存在作为独立页面 fallback。
> - **BUG 修复**：selectCiEmp / selectCoEmp 完整填充员工信息（ciSearchResults/coSearchResults 缓存 + dataset.empId + 员工信息展示区）。
> - **新增 Repair API**：`POST /api/v1/bookings/repair-employee-names` + PageHeader「修复姓名关联」按钮，用于修复 DormBooking.EmployeeName 与 SysEmployee.RealName 不一致历史数据。
> - Booking 模块原型 100% 对齐状态保持 ✅（v2.13.32/33 弹窗化重构 + BUG 修复后仍 100% 对齐）。

---

## 1. 对照总览

### 1.1 已实现的 Razor 页面（26 个）

| # | HTML 原型 | Razor 视图 | 字段一致性 | 跳转一致性 | 按钮一致性 |
|---|-----------|-----------|-----------|-----------|-----------|
| 1 | `index.html` | `Index.cshtml` | ✅ | ✅ | ✅ |
| 2 | `personnel/list.html` | `Personnel/Index.cshtml` | ✅（v2.13.86 性别列） | ✅ | ✅ |
| 3 | `personnel/create.html` | `Personnel/Create.cshtml` | ✅（v2.13.86 性别字段） | ✅ | ✅ |
| 4 | `personnel/edit.html` | `Personnel/Edit.cshtml` | ✅（v2.13.86 性别字段） | ✅ | ✅ |
| 5 | `personnel/import.html` | `Personnel/Import.cshtml` | ✅（v2.13.86 性别列 11→12） | ✅ | ✅ |
| 5.5 | `personnel/details.html` | `Personnel/Details.cshtml` | ✅（v2.13.86 新增） | ✅ | ✅ |
| 6 | `billing/standards.html` | `BillingStandard/Index.cshtml` | ✅ | ✅ | ✅ |
| 7 | `billing/standard-form.html` | `BillingStandard/Create.cshtml` / `BillingStandard/Edit.cshtml` | ✅ | ✅ | ✅ |
| 8 | `billing/dorm-bills.html` | `DormBilling/Index.cshtml` | ✅ | ✅ | ✅ |
| 9 | `billing/employee-bills.html` | `EmployeeBilling/Index.cshtml` | ✅（v2.13.86 性别列） | ✅ | ✅ |
| 10 | `booking/index.html` | `Booking/Index.cshtml` | ✅（v2.13.86 性别列） | ✅ | ✅ |
| 11 | `booking/check-in.html` | `Booking/Index.cshtml`（`checkInModal`）+ `Booking/CheckIn.cshtml`（fallback） | ✅（v2.13.86 性别字段） | ✅ | ✅ |
| 12 | `booking/check-out.html` | `Booking/Index.cshtml`（`checkOutModal`）+ `Booking/CheckOut.cshtml`（fallback） | ✅（v2.13.86 性别字段） | ✅ | ✅ |
| 13 | `booking/edit.html` | `Booking/Edit.cshtml` | ✅（v2.13.86 性别 Badge） | ✅ | ✅ |
| 14 | `dorms/list.html` | `Dorms/Index.cshtml` | ✅（v2.13.85 派生性别字段；**v2.13.95 派生班次列**） | ✅ | ✅ |
| 15 | `dorms/create.html` | `Dorms/Create.cshtml` | ✅ | ✅ | ✅ |

### 1.2 托盘 SettingsForm 一致性（v2.13.4 新增）

> 托盘 SettingsForm 非 Razor 视图，而是 WinForm，但需求规格 57 §3.2.1 11 项字段必须与 AppConfig 模型一一对应。

| # | 需求字段（57 §3.2.1） | SettingsForm 控件 | AppConfig 模型 | 一致性 |
|---|----------------------|------------------|---------------|--------|
| 1 | API 端口 | `_numApiPort` (NumericUpDown, 1024-65535) | `Tray.ApiPort` | ✅ |
| 2 | Admin 端口 | `_numAdminPort` (NumericUpDown, 1024-65535) | `Tray.AdminPort` | ✅ |
| 3 | Api 可执行文件路径 | `_txtApiPath` + `_btnApiBrowse` | `Tray.ApiExecutable` | ✅ |
| 4 | Admin 可执行文件路径 | `_txtAdminPath` + `_btnAdminBrowse` | `Tray.AdminExecutable` | ✅ |
| 5 | 数据库类型 | `_cmbProvider` (SqlServer/Sqlite) | `Database.Provider` | ✅ |
| 6 | SQL Server 连接串 | `_txtConnStr` (Multiline 56px) | `Database.ConnectionString` | ✅ |
| 7 | SQLite 数据库路径 | `_txtSqlitePath` + `_btnSqliteBrowse` | `Database.SqlitePath` | ✅ |
| 8 | 图片存储根路径 | `_txtImageRoot` + `_btnImageBrowse` | `Storage.ImageRoot` | ✅ |
| 9 | 启动时自动启动服务 | `_chkAutoStart` | `Tray.AutoStartServices` | ✅ |
| 10 | 异常时自动重启 | `_chkAutoRestart` | `Tray.AutoRestartOnCrash` | ✅ |
| 11 | 健康检查间隔（秒） | `_numHealthInterval` (5-300) | `Tray.HealthCheckIntervalSeconds` | ✅ |

**按钮一致性（57 §3.2.2）**：

| 按钮 | SettingsForm 控件 | ProcessManager 方法 | 一致性 |
|------|------------------|---------------------|--------|
| 启动 | `btnStart` | `StartAllAsync()` | ✅ |
| 停止 | `btnStop` | `StopAllAsync()` | ✅ |
| 重启 | `btnRestart` | `RestartAllAsync()` | ✅ |
| 保存 | `btnSave` | `ConfigService.Update()` + 提示重启 | ✅ |
| 取消 | `btnCancel` / X / ESC | `DialogResult.Cancel` + Close | ✅ |

**OwnerForm 机制一致性（v2.13.4 强制）**：

| 调用点 | 是否传 Owner | 是否 try-catch | 一致性 |
|--------|-------------|---------------|--------|
| `ShowSettings()` → `form.ShowDialog(_ownerForm)` | ✅ | ✅ | ✅ |
| `ShowAbout()` → `form.ShowDialog(_ownerForm)` | ✅ | ✅ | ✅ |
| `OpenLogsFolder()` 失败 → `MessageBox.Show(_ownerForm, ...)` | ✅ | ✅ | ✅ |
| `ToggleAutoStart()` → `MessageBox.Show(_ownerForm, ...)` | ✅ | ✅ | ✅ |
| 退出确认 → `SafeShow(() => MessageBox.Show(_ownerForm, ...))` | ✅ | ✅ 三层兜底 | ✅ |
| 16 | `dorms/edit.html` | `Dorms/Edit.cshtml` | ✅ | ✅ | ✅ |
| 17 | `dorms/details.html` | `Dorms/Details.cshtml` | ✅ | ✅ | ✅ |
| 18 | `dorms/history.html` | `Dorms/History.cshtml` | ✅ | ✅ | ✅ |
| 19 | `meter/index.html` | `Meter/Index.cshtml` | ✅ | ✅ | ✅ |
| 20 | `meter/entry.html` | `Meter/Entry.cshtml` | ✅ | ✅ | ✅ |
| 21 | `meter/edit.html` | `Meter/Edit.cshtml` | ✅ | ✅ | ✅ |
| 22 | `meter/import.html` | `Meter/Import.cshtml` | ✅ | ✅ | ✅ |
| 23 | `basics/index.html` | `Basics/Index.cshtml` | ✅ | ✅ | ✅ |
| 24 | `settings/index.html` | `Settings/Index.cshtml` | ✅（v2.13.95 用户管理 Tab 补「有效期至」4 态 Badge 列 + datetime-local 字段） | ✅ | ✅ |
| **25** | ~~`dorms/details.html`~~ | ~~`Dorms/Details.cshtml`~~ | — | — | — |
| **26** | `profile/index.html` | `Account/Profile.cshtml` | ✅ | ✅ | ✅ |
| **27** | `settings/about.html` | `Settings/Index.cshtml?tab=about`（嵌入 tab-about 顶部 card-license 区块） | ✅ | ✅ | ✅ |
| **28** | `settings/license-form-tray.html` | `DormManage.TrayApp/Forms/LicenseForm.cs`（托盘弹窗 WinForms） | ✅ | ✅ | ✅ |

> **v2.13.24 修正**：原表第 25 行 `dorms/details.html` 与第 17 行为同一页面（重复条目），予以删除。实际为 25 个原型 ↔ 25 个 Razor 视图（含 `Pages/Account/Profile.cshtml` 个人中心页属 settings Tab，未单列）。
>
> **v2.13.50 修正**：新增第 26 行 `profile/index.html` 原型（v2.13.50 新建于 `00-方案文档/04-HTML原型/profile/`），与 `Account/Profile.cshtml` Razor 页面对应（v2.13.49 重构：3 Tab → 8 子菜单）。实际为 **26 个原型 ↔ 26 个 Razor 视图**。
>
> **v2.13.94 修正**：新增第 27 + 28 行软件注册授权原型：
> - 第 27 行 `settings/about.html` 是 v2.13.94 新建的**独立预览页**，对应 Razor 视图通过 tab-about 区块嵌入到 `Settings/Index.cshtml`（不新建独立 Razor 页 — 避免主入口割裂）
> - 第 28 行 `settings/license-form-tray.html` 是 v2.13.94 新建的**托盘弹窗原型**（仿原 NPGS.Register RegisterForm.cs），对应 WinForms `LicenseForm.cs`（DormManage.TrayApp 项目）
> - 实际为 **28 个原型 ↔ 26 个 Razor 视图 + 1 个 WinForms 窗体**（多 UI 端共用 DormManage.Shared.Register.RegisterSdk）

### 1.3 列表页筛选区统一改造对照（v2.13.20 新增）

统一 `filter-card` / `filter-row flex-nowrap` / `filter-btn 90×38px` / `FilterPersistence.resetFilters('{module}')` 后，7 个核心列表页与原型对应关系如下：

| # | HTML 原型 | Razor 视图 | 筛选区结构 | 重置方式 | 房号 combobox | 备注 |
|---|-----------|-----------|-----------|----------|---------------|------|
| 1 | `dorms/list.html` | `Dorms/Index.cshtml` | 楼栋/楼层/状态/关键词 + 查询/重置 | JS 重置 | — | 关键词字段移除内联宽度，依赖全局样式 |
| 2 | `meter/index.html` | `Meter/Index.cshtml` | 月份/楼栋/抄表员/状态/房号/地址关键词 + 查询/重置 | JS 重置 | ✅ `dormListMeter` | 房号/地址拆分，地址做关键词模糊 |
| 3 | `booking/index.html` | `Booking/Index.cshtml` | 登记类型/开始日期/结束日期/员工/房号/状态 + 查询/重置 | JS 重置 | ✅ `dormListBooking` | 员工/日期硬编码宽度移除 |
| 4 | `personnel/list.html` | `Personnel/Index.cshtml` | 关键词/部门/员工类型/班组/考勤班次/在职状态/住宿状态/宿舍 + 查询/重置 | JS 重置 | ✅ `dormListPersonnel` | 宿舍 combobox 含所有启用房号 |
| 5 | `billing/standards.html` | `BillingStandard/Index.cshtml` | 适用类型/状态/标准名称 + 查询/重置 | JS 重置 | — | 新增"适用类型"筛选项；补 label；移除 `form-control-sm` |
| 6 | `billing/dorm-bills.html` | `DormBilling/Index.cshtml` | 计费月份/楼栋/楼层/宿舍房号/统计 + 查询/重置 | JS 重置 | ✅ `dormListDormBilling` | 统计字段使用 `.filter-item-stats` |
| 7 | `billing/employee-bills.html` | `EmployeeBilling/Index.cshtml` | 计费月份/员工/部门/员工类型/住宿状态/宿舍房号/分摊合计 + 查询/重置 | JS 重置 | ✅ `dormListEmployeeBilling` | v2.13.20 新增部门/员工类型/住宿状态筛选；分摊合计用 `.filter-item-stats` |

**全局样式源**：所有列表页筛选区样式统一收敛到 `DormManage.Admin/Pages/Shared/_Layout.cshtml`，包括 `.filter-item-stats`（min-width 90px / max-width 140px）、`.filter-item-team`（min-width 108px）、按钮 hover/focus 高优先级选择器、768px 响应式 2 列布局、底部滚动条。各列表页不再保留重复 `@section Styles` 筛选 CSS。

**持久化接入**：`filter-persistence.js` 已在 `_Layout.cshtml` 全局引入，所有 `data-filter-form` 属性表单自动初始化；重置按钮统一调用 `FilterPersistence.resetFilters('{module}')` 清空 localStorage 并提交空表单。

**总计**：25 个原型页面 ↔ 25 个 Razor 视图，全部一致；新增 7 个列表页筛选区基线条目。

---

## 2. 新增页面详细说明

### 2.1 Personnel/Import.cshtml（人员导入页面）

**原型来源**：`personnel/import.html`

**功能说明**：
- 三步导入向导：下载模板 → 上传文件 → 导入结果
- 模板包含字段：工号、姓名、部门、员工类型、考勤班次、手机号、入职日期、离职日期、房号、备注
- 导入结果展示：成功/失败/重复跳过统计 + 错误详情表格

**PageModel 特性**：
- `OnPostUploadAsync`：处理文件上传和校验
- `ImportResultDto`：导入结果数据传输对象
- `ImportErrorDto`：导入错误信息

### 2.2 DormBilling/Index.cshtml（宿舍账单页面）

**原型来源**：`billing/dorm-bills.html`

**功能说明**：
- 宿舍月度账单列表：房号、楼栋、楼层、三表用量/费用、合计、在住人数、状态
- 筛选条件：计费月份、房号、楼栋、楼层
- 操作按钮：生成、重生成、导出Excel
- 详情弹窗：费用组成 + 员工分摊明细（含部门、员工类型、考勤班次列）

**PageModel 特性**：
- `DormBillingDto`：宿舍账单数据传输对象
- 分页支持：`PagedResult<DormBillingDto>`
- 用量计算：冷水费 + 热水费 + 电费 = 合计

### 2.3 Dorms/History.cshtml（宿舍住宿历史页面）

**原型来源**：`dorms/history.html`

**功能说明**：
- 时间线式历史记录展示
- 左侧：宿舍信息卡片 + 累计住宿人次统计
- 右侧：入住/退宿时间线（在宿/已退宿 Badge）
- 每条记录：员工工号/姓名/部门 + 入住/退宿日期 + 居住天数

**PageModel 特性**：
- 路由参数：`{id:int}` 宿舍 ID
- 从 `DormBooking` 表查询历史记录
- `HistoryRecordDto`：历史记录数据传输对象

### 2.4 Meter/Entry.cshtml（手动补录抄表页面）

**原型来源**：`meter/entry.html`

**功能说明**：
- 宿舍 + 月份选择 → 三表读数录入 + 照片上传
- 用量实时预览（右侧面板）
- 上月读数参考（左侧面板）
- 覆盖模式：同月同房号已存在记录时覆盖更新

**PageModel 特性**：
- `OnPostAsync`：创建或覆盖智能抄表记录
- 自动判定状态：`MeterRecord.DetermineStatus(cold, hot, electric)`
- 已修正记录（status=2）不可再修改

### 2.5 Meter/Edit.cshtml（修正抄表读数页面）

**原型来源**：`meter/edit.html`

**功能说明**：
- 只读展示原始记录信息（宿舍、月份、抄表员、设备）
- 三表读数修改 + 照片重新上传
- 修正原因必填
- 用量变化对比表（原读数 vs 新读数 vs 变化）

**PageModel 特性**：
- 路由参数：`{id:int}` 记录 ID
- `OnPostAsync`：修正记录并标记 status=2（已修正）
- 修正后记录不可再修改或删除

### 2.6 Meter/Import.cshtml（批量导入抄表页面）

**原型来源**：`meter/import.html`

**功能说明**：
- 三步导入向导：下载模板 → 上传文件 → 导入结果
- 模板列：房号、抄表月份、冷水读数、热水读数、电表读数、抄表员、备注
- 校验规则：覆盖模式（status=0/4 可覆盖，status=1/2 需修正）
- 错误详情：行号、房号、月份、三表读数、错误信息

**PageModel 特性**：
- `OnPostUploadAsync`：文件上传和校验
- `OnPostImportAsync`：确认导入
- `ImportResultDto`：导入结果统计

### 2.7 Settings/Index.cshtml（系统设置页面）

**原型来源**：`settings/index.html`

**功能说明**：
- 8 个 Tab 页签：服务与端口、数据库连接、PDA App 版本、用户管理、角色与权限、备份与恢复、系统集成、**关于系统**（v2.13.94 起顶部新增「软件注册授权」card-license 区块）
- 服务与端口：PDA/Web 端口配置 + 启动/停止 + 域名 + 图片路径
- 数据库连接：服务器/端口/库名/账号/密码 + 8 步深度验证
- PDA App 版本：版本列表 + 发布新版本
- 用户管理：用户增删改、启停、重置密码
- 角色与权限：角色增删改、权限矩阵配置
- 备份与恢复：自动备份策略 + 手动备份列表
- 系统集成：HR/K3ERP 双系统配置 + 连接测试

**v2.13.94 关于系统 Tab 顶部新增 card-license 区块**（嵌入主入口，不新建独立 Razor 页）：
- 5 字段：机器码（只读 24 hex）/ 公司名称（只读）/ 注册码（29 位 5-5-5-5-5）/ 有效日期（HEX 解码）/ 试用次数
- 4 按钮：复制机器码 / 注册 / 取消注册 / 清理
- 状态 Badge：`已注册`（绿）/ `已过期`（橙）/ `未注册`（红）三态切换
- 数据源：`DormManage.Shared.Register.RegisterSdk`（与托盘端共用同一 SDK）
- 详见原型 `settings/about.html`（独立预览页，与本 tab-about 区块字段完全一致）
- 关于系统：版本信息 + 技术栈 + 设计规模

**PageModel 特性**：
- 所有配置数据使用 DTO 模拟
- 8 步数据库深度验证（TCP → SQL 登录 → 库可见 → CREATE → INSERT → SELECT → UPDATE → DELETE）

---

## 3. 数据库模型补充

新增页面涉及的数据库模型：

| 模型 | 表名 | 用途 |
|------|------|------|
| `SysEmployee` | `SysEmployee` | 员工档案（导入目标） |
| `Dorm` | `Dorm` | 宿舍档案 |
| `MeterRecord` | `MeterRecord` | 智能抄表 |
| `DormBooking` | `DormBooking` | 办理登记（历史记录来源） |
| `Building` | `Building` | 楼栋 |
| `Floor` | `Floor` | 楼层 |

---

## 4. 版本历史

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v2.12.0 | 2026-07-12 | 初始基线对照报告 |
| v2.12.19 | 2026-07-13 | 基础资料新增「员工班组」分类 |
| **v2.12.20** | **2026-07-13** | **补全 7 个缺失 Razor 页面** |
| **v2.12.21** | **2026-07-13** | ~~宿舍详情布局调整（基本信息 250px + 操作 250px 上下对齐）~~ ⚠️ 已被 v2.13.21「标签与数据强制同一行」取代 |
| **v2.12.22** | **2026-07-13** | ~~宿舍详情布局优化（宽度 300px + 当前入住人员顶部与基本信息顶部水平对齐）~~ ⚠️ 已被 v2.13.21 取代 |
| **v2.12.23** | **2026-07-13** | ~~宿舍详情布局回归左右两栏~~ ⚠️ 保留左右两栏结论，但基本信息内部显示规则以 v2.13.21 为准 |
| **v2.12.24** | **2026-07-13** | ~~宿舍详情布局固定（左栏基本信息+操作各300px固定宽度上下堆叠）~~ ⚠️ 外部两栏 300px 保留，基本信息内部以 v2.13.21「标签+数据同一行」为准 |
| **v2.12.25** | **2026-07-13** | 宿舍详情 BUG 修复（左右两栏严格并排，禁止 Bootstrap row 默认换行） |
| **v2.12.26** | **2026-07-13** | **新增人员表单紧凑规范（去掉规范信息、减小行间距、一屏完整展示）** |
| **v2.12.27** | **2026-07-13** | **新增/编辑人员新增班组字段 + 房号只读字段；编辑人员布局与新增人员完全一致** |
| **v2.12.28** | **2026-07-13** | **办理入住/退房表单紧凑规范（去掉规范信息、姓名模糊筛选、减小行间距、一屏完整展示）** |
| **v2.12.29** | **2026-07-13** | **办理登记按钮改为模态弹窗（办理入住/退房各弹独占窗口；姓名动态模糊筛选工号/姓名/手机号；操作类型仅显示对应项；日期默认服务端当前日期）** |
| **v2.12.30** | **2026-07-13** | **办理登记模态弹窗校验信息同行布局 + 校验未通过锁定房间选择和提交按钮** |
| **v2.12.31** | **2026-07-13** | **办理退房模态弹窗校验提示语规范（"校验提示：员工当前已在宿 D-178，可以办理退房" / "校验未通过：xxx"）** |
| **v2.12.32** | **2026-07-13** | **办理退房模态弹窗校验信息合并 + 单行展示（"退房校验：校验通过！退房日期 2026-08-28 ，员工当前已在宿D-178，可以办理退房"）** |
| **v2.12.33** | **2026-07-13** | **办理退房模态弹窗校验信息房号加大红色字体加粗（`text-danger fw-bold fs-5`）** |
| **v2.13.20** | **2026-07-19** | **统一 7 个列表页筛选区布局与风格：filter-card/flex-nowrap/filter-btn 90×38px；房号统一 combobox；重置接入 FilterPersistence；EmployeeBilling 补齐部门/员工类型/住宿状态；BillingStandard 补齐适用类型；页内重复 CSS 清理至 _Layout.cshtml** |
| **v2.13.21** | **2026-07-19** | **宿舍详情 `Dorms/Details.cshtml` 基本信息区域标签与数据强制同一行显示；清理 v2.12.21-v2.12.25 过时布局描述** |

---

## 附录：v2.13.x 重大基线变更

### A. v2.13.3 — 25 项差距全部完成（2026-07-15）

| # | 类别 | 变更 | 关联页面/文件 |
|---|------|------|--------------|
| **P0** | 部署 | DormManage.TrayApp 源码补全（11 文件） | `DormManage.TrayApp/` |
| P1-1 | UI | MenuViewComponent.Default.cshtml 补全（菜单权限动态渲染） | `Pages/Shared/Components/Menu/Default.cshtml` |
| P1-2 | UI | /Settings/User 独立页面（CRUD） | `Pages/Settings/User.cshtml` | ~~⚠️ v2.13.13 已合并为 Settings/Index.cshtml 内嵌 tab~~ |
| P1-3 | UI | /Settings/Role 独立页面 + 权限矩阵 | `Pages/Settings/Role.cshtml` | ~~⚠️ v2.13.13 已合并为 Settings/Index.cshtml 内嵌 tab~~ |
| P1-4 | UI | 首页数据看板 7 个图表（Chart.js） | `Pages/Index.cshtml` |
| P1-5 | 服务 | DashboardService 聚合服务 | `Shared/Services/DashboardService.cs` |
| P1-6 | 模型 | 抄表状态枚举新增 Unfinished + Voided | `Shared/Models/MeterRecord.cs` |
| P1-7 | API | PDA 上传接口 | `Api/Controllers/Pda/PdaController.cs` |
| P1-8 | API | 抄表作废 + 撤销作废 | `Meter/MeterController.cs` |
| P1-9 | 服务 | ConfirmReservedCheckOutAsync 完整实现 | `Shared/Services/BookingService.cs` |
| P1-10 | 服务 | 数据库深度验证 6 步 | `Shared/Services/DatabaseHealthService.cs` |
| P1-11 | API | 服务启停 API + IPC 通道 | `Shared/Services/ServiceIpc.cs` |
| P1-12 | API | 备份恢复（SQLite + SQL Server 双适配） | `Api/Controllers/System/BackupController.cs` |
| P1-13 | API | 用户/角色 CRUD API | `Api/Controllers/Auth/UserController.cs` |
| P1-14 | API | 人员清单 Excel 导入导出 | `Api/Controllers/Personnel/PersonnelController.cs` |
| P1-15 | UI | DormBilling/EmployeeBilling 详情页 | `Pages/DormBilling/Details.cshtml` |
| P1-16 | 模型 | SysPermission 字段补全（Description/IsSystem/UpdatedAt/CreatedBy） | `Shared/Models/SysPermission.cs` |
| P2-1 | UI | 共用页头 + Tab 导航组件 | `ViewComponents/PageHeaderViewComponent.cs` |
| P2-2 | UI | 首页月度选择器 | `Pages/Index.cshtml` |
| P2-3 | UI | 办理编辑页员工信息区扩展 | `Pages/Booking/Edit.cshtml` |
| P2-4 | 模型 | Dorm.BedNumbers 床位号字段 | `Shared/Models/Dorm.cs` |
| P2-5 | 服务 | 宿舍容量变更约束 | `Shared/Services/DormService.cs` |
| P2-6 | API | 系统集成（HR/K3ERP）CRUD + 测试连接 | `Api/Controllers/System/IntegrationController.cs` |
| P2-7 | API | PDA App 版本管理 + APK 上传/下载 | `Api/Controllers/Pda/AppVersionController.cs` |
| P2-8 | 服务 | 人员班组字段 + 筛选 | `Shared/Models/SysEmployee.cs` + `PersonnelService.cs` |
| P2-9 | 工具 | 通用 CSV 导出器 | `Shared/Services/CsvExporter.cs` |
| **新增** | 服务 | DormManage.TrayApp IPC Server 集成（接收 Web Admin 命令） | `TrayApp/TrayAppContext.cs` |

### B. v2.13.x 新增 Razor 视图

| # | Razor 视图 | 用途 |
|---|------------|------|
| ~~26~~ | ~~`Pages/Settings/User.cshtml`~~ | ~~用户管理~~ | ~~⚠️ v2.13.13 已合并为 Settings/Index.cshtml 内嵌 tab~~ |
| ~~27~~ | ~~`Pages/Settings/Role.cshtml`~~ | ~~角色管理~~ | ~~⚠️ v2.13.13 已合并为 Settings/Index.cshtml 内嵌 tab~~ |
| 28 | `Pages/DormBilling/Details.cshtml` | 宿舍账单详情 |
| 29 | `Pages/EmployeeBilling/Details.cshtml` | 员工账单详情 |
| 30 | `Pages/Shared/Components/Menu/Default.cshtml` | 菜单权限视图 |
| 31 | `Pages/Shared/Components/PageHeader/Default.cshtml` | 共用页头视图 |

### C. v2.13.x 新增 API 端点（30+ 端点）

| 控制器 | 端点数 | 主要功能 |
|--------|--------|---------|
| `Api/Auth/UserController.cs` | 8 | 用户/角色 CRUD |
| `Api/Pda/PdaController.cs` | 3 | PDA 上传/图片/覆盖率 |
| `Api/Pda/AppVersionController.cs` | 8 | PDA 版本管理 |
| `Api/Personnel/PersonnelController.cs` | 3 | 人员查询/导入/导出 |
| `Api/System/SystemController.cs` | 4 | 服务启停 |
| `Api/System/BackupController.cs` | 5 | 备份恢复 |
| `Api/System/DbHealthController.cs` | 2 | 数据库深度验证 |
| `Api/System/IntegrationController.cs` | 7 | 系统集成 |
| **合计** | **40** | — |

### D. v2.13.x 编译状态

```
✅ dotnet build DormManage.sln -c Debug
   0 个错误
   警告：~5 个（CS8604/CS8602 null 引用提示，不影响功能）
```

### E. v2.13.x BUG 修复清单

| BUG # | 描述 | 修复位置 |
|-------|------|---------|
| #1 | MenuViewComponent.Default.cshtml 缺失 | 新增 cshtml |
| #2 | ConfigService.PropertyNameCaseInsensitive | ConfigService.cs |
| #3 | SysEmployee 字段错误（无 Gender/Team/IdCard） | 移除错误 + 补充 Team |
| #4 | PagedResult.Page/PageSize 字段错误 | UserController 等 |
| #5 | namespace 冲突 | global:: 前缀 |
| #6 | Nullable.HasValue 推断错误 | Booking Edit |
| #7 | Dorm.FloorNo 不存在 | 改 FloorId |
| #8 | AppVersion DbSet 缺失 | DormDbContext |
| #9 | TrayAppContext 游离代码块 | 重写文件 |
| **#10 (v2.13.4)** | **托盘右键 → 系统设置 "UI异常，创建窗口出错"** | **TrayAppContext.cs（内嵌 OwnerForm + MainForm）+ NotifyIconManager.cs（owner 参数 + SafeMenuFont + SafeInvoke）+ SettingsForm.cs（拆分 InitializeControls/BuildUI + 整体 try-catch）+ AboutForm.cs（三层 Font 兜底）** |
| #10 | TrayApp 缺 Shared 引用 | csproj |

**v2.13.3 完成**：25 项差距清单全部清零 ✅

## v2.13.109 备注：SQLite Provider 彻底移除

自 v2.13.109 起，DormManage 运行时仅支持 SQL Server。SQLite 代码路径、EF Core SQLite Provider、SQLite 备份恢复逻辑均已移除。历史配置中的 SqlitePath 字段仅为旧配置反序列化兼容，不代表运行时继续支持 SQLite。生产数据库初始化以 init_schema.sql 和 SQL Server 运维脚本为准，不使用 EnsureCreated()。
