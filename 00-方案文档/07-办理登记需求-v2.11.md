# 需求规格说明书 — v2.11 增补（办理登记，原"住宿记录"）

> **范围**：将原"住宿记录"升级为"办理登记"，强化 CRUD + 校验联动  
> **日期**：2026-07-10  
> **更新**：2026-07-11（v2.11.2 增补：数据完整性补充）  
> **更新**：2026-07-12（v2.11.5：字段描述与列表表头"办理日期"统一改为"入退日期"）  
> **更新**：2026-07-12（v2.11.5：新增 §13 修改办理登记页面字段数据来源与关系矩阵）  
> **更新**：2026-07-12（v2.11.5.b：原型 edit.html 数据关联可视化样式规范化——双卡片布局 + 图标 tooltip 替代错乱徽章）  
> **更新**：2026-07-12（v2.11.6：业务术语统一，"预订"→"预约"；办理登记列表新增"登记时间"列）  
> **更新**：2026-07-12（v2.11.7：操作列"编辑"→"修改"；修改按钮仅 Status=1 预约时可点，其他状态锁定）  
> **更新**：2026-07-12（v2.11.7.b：只读"修改"按钮去掉 🔒 图标，仅显示"修改"文本）  
> **更新**：2026-07-12（v2.11.8：操作列新增"入住"快速确认按钮（Type=1 入住 && Status=1 预约 → 即时变更为在宿，同步登记人/登记时间）；新增 API POST /api/v1/bookings/{id}/confirm-checkin）
> **更新**：2026-07-12（v2.11.15.d：修改办理登记页面重构——移除数据关联可视化区（ER 图/字段矩阵/字典/校验/写入流程），改为参照"编辑人员"页面的标准表单布局：顶部只读信息摘要 + 标准表单控件（入退日期/原因/状态/备注） + 系统自动记录区 + 操作按钮）  
> **更新**：2026-07-12（v2.11.8.b：入住按钮的房间床位余量校验文案优化——无余量时提示"床位已满，请更换其他房间"）  
> **更新**：2026-07-12（v2.11.9：操作列按钮位置调整——"入住"按钮由"修改"左侧调整为右侧，最终顺序：修改 → 入住 → 退房）  
> **更新**：2026-07-12（v2.11.10：操作列新增"撤销"按钮——撤销今日退房，位置在"修改"右侧；新增 API POST /api/v1/bookings/{id}/undo-checkout；无余量时提示"床位已满，撤销退房失败！"）  
> **更新**：2026-07-12（v2.11.11：操作列**最右侧**新增"撤销"按钮——撤销预约（Status=1 预约 → 4 已取消）；新增 API POST /api/v1/bookings/{id}/cancel-reservation；红色 btn-outline-danger；无需校验房间余量）  
> **更新**：2026-07-12（v2.11.12：操作列"退房"按钮显示条件扩展——新增 Type=2 退房 && Status=1 预约时显示，位置在修改与撤销预约之间；新增 API POST /api/v1/bookings/{id}/confirm-checkout；联动 SysEmployee.DormCode=NULL）  
> **更新**：2026-07-12（v2.11.13：操作按钮点击后即时刷新规则——点击"退房/入住/撤销"任一按钮成功后，前端即时刷新列表显示当前记录的最新数据；Razor 用 `location.reload()`，原型用 `registerList.reset()`；失败时不刷新）  
> **更新**：2026-07-12（v2.11.14：BUGFIX——列表筛选"房号"条件 JS 运算符优先级 BUG 修复；改用 `indexOf === -1` 显式判断；新增 §14 BUGFIX 章节含根因分析）  
> **更新**：2026-07-13（v2.11.17：操作列新增"撤销"按钮（在宿场景）+ "退房"按钮行为变更——入住+在宿记录的退房改为创建新退房记录模式）  
> **更新**：2026-07-13（v2.12.10：筛选条件持久化——所有筛选区字段值自动保存到 localStorage，切换 Tab 页签 / 刷新页面后自动恢复；重置按钮改为 JS 调用而非整页跳转）  
> **更新**：2026-07-13（v2.12.35：床位号容量变更约束规则——容量减少时检查已入住人数≥新容量则禁止更改；容量减少时自动重新分配超床位号员工；容量增加时不影响原床位号）
> **更新**：2026-07-13（v2.12.34：办理入住弹窗校验信息移至"操作类型"右侧同行显示——左栏操作类型固定 100px，右栏校验信息；校验失败时锁定房间选择和提交按钮；入住日期 > 当前日期时操作类型显示"入住"/"预约"二选一；校验通过后按优先级（1.同班次 2.入住率高+余量 3.同部门）预分配房号）
> **位置**：替代 06-需求中 §4.2 住宿记录章节

---

## 1. 业务背景

### 1.1 名称变更

| 旧名 | 新名 |
|------|------|
| 住宿记录 | **办理登记** |

### 1.2 功能升级

原「住宿记录」只展示员工的入住/退宿状态，本次升级为完整 CRUD：
- 可对单个记录进行新增、修改、删除
- 按员工姓名/工号/手机号模糊查询
- 选择员工后自动带出手机号 + 部门
- 操作类型（入住 / 退房）显式区分
- 房间列表自动过滤「有余量」+「日期不冲突」的房间
- 严格的一进一出日期重叠校验

---

## 2. 业务规则

### 2.1 一进一出日期重叠校验（核心）

| 规则 | 说明 |
|------|------|
| R-BOOK-01 | **同一员工**的办理登记按时间排序必须交替：入住 → 退房 → 入住 → 退房 ... |
| R-BOOK-02 | 同一员工的**同日不能既有入住又有退房**（重叠校验） |
| R-BOOK-03 | 新入住日期必须 **≥ 上一次退房日期 + 1 天**（一进一出顺序） |
| R-BOOK-04 | 新退房日期必须 **≥ 上一次入住日期** 且 **≤ 下一次入住日期 - 1 天** |
| R-BOOK-05 | 入住日期不能早于员工入职日期 |
| R-BOOK-06 | 退房日期不能晚于今天 |

### 2.2 房间余量校验

| 规则 | 说明 |
|------|------|
| R-BOOK-07 | 入住时房间余量 = 房间容量 - 当前在宿人数 - **预约入住**人数 |
| R-BOOK-08 | 退房时记录必须存在且状态为「在宿」 |
| R-BOOK-09 | 退房后该记录状态变为「已退房」，员工变为未分配宿舍 |

### 2.3 状态机

```
[预约入住] --实际入住日到达--> [在宿]
[在宿]    --办理退房---------> [已退房]（创建 Type=2 退房新记录）
[在宿]    --今日撤销---------> [已取消]（仅 BookingDate=今天，Type=1 入住 && Status=2 在宿）
[预约入住] --取消-------------> [已取消]
[预约退房] --实际退房--------> [已退房]
[预约退房] --取消-------------> [已取消]
```

---

## 3. 数据模型（替代 DormResidency）

### 3.1 DormBooking 办理登记表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| Id | INT | PK | 主键自增 |
| EmployeeId | INT | 是 | FK→SysEmployee |
| EmployeeCode | NVARCHAR(20) | 是 | 冗余（便于查询） |
| EmployeeName | NVARCHAR(50) | 是 | 冗余 |
| Phone | NVARCHAR(20) | 否 | 冗余 |
| Department | NVARCHAR(50) | 否 | 冗余 |
| DormCode | NVARCHAR(20) | 是 | FK→Dorm.DormCode |
| **Type** | TINYINT | 是 | **1=入住 2=退房** |
| **BookingDate** | DATE | 是 | **预计/实际入退日期**（前台录入） |
| **Status** | TINYINT | 是 | **1=预约 2=在宿 3=已退房 4=已取消** |
| Reason | NVARCHAR(200) | 否 | 原因（入职、调岗、离职、换房等） |
| Remark | NVARCHAR(500) | 否 | 备注 |
| **RegistrationDate** | DATETIME | 是 | **实际登记日期（默认 now()）** |
| **Registrar** | NVARCHAR(50) | 是 | **登记人（当前登录用户名）** |
| CreatedAt | DATETIME | 是 | — |
| UpdatedAt | DATETIME | 否 | — |

**索引**：
- `IX_DormBooking_Employee` (EmployeeId, BookingDate DESC)
- `IX_DormBooking_Dorm` (DormCode, BookingDate DESC)
- `UX_DormBooking_EmployeeTypeDate` UNIQUE (EmployeeId, Type, BookingDate) — 防止同一员工同一天重复登记
- `IX_DormBooking_Status` (Status, BookingDate)

---

## 4. API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/bookings | 列表（支持员工模糊筛选 + 分页） |
| GET | /api/v1/bookings/{id} | 详情 |
| POST | /api/v1/bookings/check-in | 办理入住 |
| POST | /api/v1/bookings/{id}/check-out | 办理退房 |
| **POST** | **/api/v1/bookings/{id}/confirm-checkin** | **快速确认入住（v2.11.8 新增）：Type=1 入住 && Status=1 预约 → 即时变更为 Status=2 在宿，并同步更新 Registrar=当前账号、RegistrationDate=now()、UpdatedAt=now()** |
| **POST** | **/api/v1/bookings/{id}/undo-checkout** | **撤销退房（v2.11.10 新增）：入退日期 == 服务端今天 && Status=3 已退房 → 后台先校验房间床位余量；有余量则即时变更为 Status=2 在宿，并同步更新 Registrar=当前账号、RegistrationDate=now()、UpdatedAt=now()；无余量返回 400 NO_CAPACITY，提示"床位已满，撤销退房失败！"** |
| **POST** | **/api/v1/bookings/{id}/cancel-reservation** | **撤销预约（v2.11.11 新增）：Status=1 预约 → 即时变更为 Status=4 已取消，并同步更新 Registrar=当前账号、RegistrationDate=now()、UpdatedAt=now()** |
| **POST** | **/api/v1/bookings/{id}/cancel-today** | **撤销在宿（v2.11.17 新增）：Type=1 入住 && Status=2 在宿 && BookingDate==今天 → 即时变更为 Status=4 已取消，并同步更新 Registrar=当前账号、RegistrationDate=now()、UpdatedAt=now()** |
| **POST** | **/api/v1/bookings/{id}/confirm-checkout-create** | **创建退房记录（v2.11.17 新增）：Type=1 入住 && Status=2 在宿 → 创建一条 Type=2 退房的新记录（BookingDate=今天），并将原记录 Status 变更为 3 已退房；弹出确认对话框展示当前日期和类型为"退房"的信息** |
| PUT | /api/v1/bookings/{id} | 修改（日期/原因/备注） |
| DELETE | /api/v1/bookings/{id} | 删除（仅 预约/已取消 状态可删） |
| GET | /api/v1/bookings/employee-search | 按员工模糊查询（用于联动下拉） |
| GET | /api/v1/bookings/available-dorms | 查询有余量的房间（按日期范围） |

### 4.1 快速确认入住接口契约（v2.11.8 新增）

**请求**：`POST /api/v1/bookings/{id}/confirm-checkin`

**请求体**：空（仅依赖路径参数 id）

**校验**（后端强制）：
| # | 校验项 | 触发条件 | 错误码 |
|---|--------|---------|--------|
| V-CONFIRM-01 | 记录存在 | id 无效 | `404 NOT_FOUND` |
| V-CONFIRM-02 | Type=1 入住 | Type≠1 | `400 INVALID_TYPE`：`仅 Type=1 入住类型可快速确认` |
| V-CONFIRM-03 | Status=1 预约 | Status≠1 | `400 INVALID_STATUS`：`仅 Status=1 预约状态可快速确认` |
| V-CONFIRM-04 | **房间床位余量** | 入住时 `Dorm.CurrentResidentCount < Dorm.Capacity` | `400 NO_CAPACITY`：**`床位已满，请更换其他房间`**（v2.11.8.b 优化提示文案） |

**响应**（成功）：

```json
{
  "success": true,
  "data": {
    "id": 12345,
    "status": 2,
    "statusName": "在宿",
    "registrar": "admin",
    "registrationDate": "2026-07-12T15:30:00",
    "updatedAt": "2026-07-12T15:30:00"
  }
}
```

**服务端逻辑**：
```
1. 加载 DormBooking[id]，若不存在 → 404
2. 若 Type ≠ 1 → 400 INVALID_TYPE
3. 若 Status ≠ 1 → 400 INVALID_STATUS
4. **校验房间床位余量**：`Dorm.CurrentResidentCount < Dorm.Capacity`
   - 满足 → 进入步骤 5（事务写入）
   - **不满足 → 返回 400 NO_CAPACITY，提示文案："床位已满，请更换其他房间"**（v2.11.8.b 优化）
5. 事务内：
   ├─ DormBooking.Status = 2
   ├─ DormBooking.Registrar = 当前登录用户名
   ├─ DormBooking.RegistrationDate = now()
   ├─ DormBooking.UpdatedAt = now()
   └─ SysEmployee.DormCode = 该记录 DormCode（联动同步）
6. 返回 ApiResponse { success: true, data: 更新后的记录 }
```

### 4.2 撤销退房接口契约（v2.11.10 新增）

**请求**：`POST /api/v1/bookings/{id}/undo-checkout`

**请求体**：空（仅依赖路径参数 id）

**校验**（后端强制）：
| # | 校验项 | 触发条件 | 错误码 |
|---|--------|---------|--------|
| V-UNDO-01 | 记录存在 | id 无效 | `404 NOT_FOUND` |
| V-UNDO-02 | Status=3 已退房 | Status≠3 | `400 INVALID_STATUS`：`仅 Status=3 已退房可撤销` |
| V-UNDO-03 | 入退日期 = 服务端今天 | BookingDate ≠ 今天 | `400 NOT_TODAY`：`仅入退日期为今天的记录可撤销` |
| V-UNDO-04 | **房间床位余量** | 入住时 `Dorm.CurrentResidentCount < Dorm.Capacity` | `400 NO_CAPACITY`：**`床位已满，撤销退房失败！`** |

**响应**（成功）：

```json
{
  "success": true,
  "data": {
    "id": 12345,
    "status": 2,
    "statusName": "在宿",
    "registrar": "admin",
    "registrationDate": "2026-07-12T15:30:00",
    "updatedAt": "2026-07-12T15:30:00"
  }
}
```

**服务端逻辑**：
```
1. 加载 DormBooking[id]，若不存在 → 404
2. 若 Status ≠ 3 → 400 INVALID_STATUS
3. 若 BookingDate ≠ today → 400 NOT_TODAY
4. **校验房间床位余量**：`Dorm.CurrentResidentCount < Dorm.Capacity`
   - 满足 → 进入步骤 5（事务写入）
   - **不满足 → 返回 400 NO_CAPACITY，提示文案："床位已满，撤销退房失败！"**（不修改任何字段）
5. 事务内：
   ├─ DormBooking.Status = 2（在宿）
   ├─ DormBooking.Registrar = 当前登录用户名
   ├─ DormBooking.RegistrationDate = now()
   ├─ DormBooking.UpdatedAt = now()
   └─ SysEmployee.DormCode = 该记录 DormCode（联动恢复）
6. 返回 ApiResponse { success: true, data: 更新后的记录 }
```

### 4.3 撤销预约接口契约（v2.11.11 新增）

**请求**：`POST /api/v1/bookings/{id}/cancel-reservation`

**请求体**：空（仅依赖路径参数 id）

**校验**（后端强制）：
| # | 校验项 | 触发条件 | 错误码 |
|---|--------|---------|--------|
| V-CANCEL-01 | 记录存在 | id 无效 | `404 NOT_FOUND` |
| V-CANCEL-02 | Status=1 预约 | Status≠1 | `400 INVALID_STATUS`：`仅 Status=1 预约可撤销` |

**响应**（成功）：

```json
{
  "success": true,
  "data": {
    "id": 12345,
    "status": 4,
    "statusName": "已取消",
    "registrar": "admin",
    "registrationDate": "2026-07-12T15:30:00",
    "updatedAt": "2026-07-12T15:30:00"
  }
}
```

**服务端逻辑**：
```
1. 加载 DormBooking[id]，若不存在 → 404
2. 若 Status ≠ 1 → 400 INVALID_STATUS
3. 事务内：
   ├─ DormBooking.Status = 4（已取消）
   ├─ DormBooking.Registrar = 当前登录用户名
   ├─ DormBooking.RegistrationDate = now()
   └─ DormBooking.UpdatedAt = now()
4. 返回 ApiResponse { success: true, data: 更新后的记录 }
```

> **与 v2.11.10 撤销退房的区别**：
> - **撤销退房**（v2.11.10）针对已退房（Status=3）记录的"误操作回退"，需校验房间余量；
> - **撤销预约**（v2.11.11）针对预约（Status=1）记录的"主动取消"，无需校验房间余量（从未入住）。

### 4.4 确认退房接口契约（v2.11.12 新增）

**请求**：`POST /api/v1/bookings/{id}/confirm-checkout`

**请求体**：空（仅依赖路径参数 id）

**校验**（后端强制）：
| # | 校验项 | 触发条件 | 错误码 |
|---|--------|---------|--------|
| V-CONFIRM-CO-01 | 记录存在 | id 无效 | `404 NOT_FOUND` |
| V-CONFIRM-CO-02 | Type=2 退房 | Type≠2 | `400 INVALID_TYPE`：`仅 Type=2 退房类型可确认` |
| V-CONFIRM-CO-03 | Status=1 预约 | Status≠1 | `400 INVALID_STATUS`：`仅 Status=1 预约状态可确认` |

**响应**（成功）：

```json
{
  "success": true,
  "data": {
    "id": 12345,
    "status": 3,
    "statusName": "已退房",
    "registrar": "admin",
    "registrationDate": "2026-07-12T15:30:00",
    "updatedAt": "2026-07-12T15:30:00"
  }
}
```

**服务端逻辑**：
```
1. 加载 DormBooking[id]，若不存在 → 404
2. 若 Type ≠ 2 → 400 INVALID_TYPE
3. 若 Status ≠ 1 → 400 INVALID_STATUS
4. 事务内：
   ├─ DormBooking.Status = 3（已退房）
   ├─ DormBooking.Registrar = 当前登录用户名
   ├─ DormBooking.RegistrationDate = now()
   ├─ DormBooking.UpdatedAt = now()
   └─ SysEmployee.DormCode = NULL（联动：员工变为未分配宿舍）
5. 返回 ApiResponse { success: true, data: 更新后的记录 }
```

> **与现有 `/api/v1/bookings/{id}/check-out` 的区别**：
> - **check-out**（既有）：针对当前在宿（Status=2）的员工，需要选员工+在宿记录+输入退房日期走 4 步表单；
> - **confirm-checkout**（v2.11.12）：针对预约退房（Type=2 && Status=1）的记录，一键确认即变更为已退房。

### 4.5 撤销在宿接口契约（v2.11.17 新增）

> **业务场景**：员工当天办理入住后，发现信息有误或需要取消（如入退日期为当天），HR 可一键"撤销"该入住记录，将其状态改为"已取消"，**不创建新记录**。

**请求**：`POST /api/v1/bookings/{id}/cancel-today`

**请求体**：空（仅依赖路径参数 id）

**校验**（后端强制）：
| # | 校验项 | 触发条件 | 错误码 |
|---|--------|---------|--------|
| V-CANCEL-TODAY-01 | 记录存在 | id 无效 | `404 NOT_FOUND` |
| V-CANCEL-TODAY-02 | Type=1 入住 | Type≠1 | `400 INVALID_TYPE`：`仅 Type=1 入住类型可撤销` |
| V-CANCEL-TODAY-03 | Status=2 在宿 | Status≠2 | `400 INVALID_STATUS`：`仅 Status=2 在宿可撤销` |
| V-CANCEL-TODAY-04 | 入退日期 = 服务端今天 | BookingDate ≠ 今天 | `400 NOT_TODAY`：`仅入退日期为今天的记录可撤销` |

**响应**（成功）：

```json
{
  "success": true,
  "data": {
    "id": 12345,
    "status": 4,
    "statusName": "已取消",
    "registrar": "admin",
    "registrationDate": "2026-07-13T10:30:00",
    "updatedAt": "2026-07-13T10:30:00"
  }
}
```

**服务端逻辑**：
```
1. 加载 DormBooking[id]，若不存在 → 404
2. 若 Type ≠ 1 → 400 INVALID_TYPE
3. 若 Status ≠ 2 → 400 INVALID_STATUS
4. 若 BookingDate ≠ today → 400 NOT_TODAY
5. 事务内：
   ├─ DormBooking.Status = 4（已取消）
   ├─ DormBooking.Registrar = 当前登录用户名
   ├─ DormBooking.RegistrationDate = now()
   └─ DormBooking.UpdatedAt = now()
   （不联动 SysEmployee.DormCode，因为原记录仍在宿，只是取消本次登记）
6. 返回 ApiResponse { success: true, data: 更新后的记录 }
```

> **与 v2.11.11 撤销预约的区别**：
> - **撤销预约**（v2.11.11）：Status=1 预约 → 4 已取消（从未入住）；
> - **撤销在宿**（v2.11.17）：Status=2 在宿 → 4 已取消（已入住，但当天撤销），**不创建新记录**。

### 4.6 创建退房记录接口契约（v2.11.17 新增）

> **业务场景**：员工当前在宿（Type=1 入住 && Status=2 在宿），点击"退房"按钮后，**弹出一个确认对话框**，展示当前日期和类型为"退房"的信息，用户确认后：
> 1. 创建一条新的 Type=2 退房类型的 DormBooking 记录（BookingDate=今天）；
> 2. 将原入住记录的 Status 变更为 3 已退房；
> 3. 联动：`SysEmployee.DormCode = NULL`（员工变为未分配宿舍）。

**请求**：`POST /api/v1/bookings/{id}/confirm-checkout-create`

**请求体**：空（仅依赖路径参数 id）

**校验**（后端强制）：
| # | 校验项 | 触发条件 | 错误码 |
|---|--------|---------|--------|
| V-CREATE-CO-01 | 记录存在 | id 无效 | `404 NOT_FOUND` |
| V-CREATE-CO-02 | Type=1 入住 | Type≠1 | `400 INVALID_TYPE`：`仅 Type=1 入住类型可创建退房记录` |
| V-CREATE-CO-03 | Status=2 在宿 | Status≠2 | `400 INVALID_STATUS`：`仅 Status=2 在宿可创建退房记录` |

**响应**（成功）：

```json
{
  "success": true,
  "data": {
    "originalRecord": {
      "id": 12345,
      "status": 3,
      "statusName": "已退房"
    },
    "newRecord": {
      "id": 12346,
      "type": 2,
      "typeName": "退房",
      "bookingDate": "2026-07-13",
      "status": 3,
      "statusName": "已退房",
      "registrar": "admin",
      "registrationDate": "2026-07-13T10:30:00"
    }
  }
}
```

**服务端逻辑**：
```
1. 加载 DormBooking[id]（原入住记录），若不存在 → 404
2. 若 Type ≠ 1 → 400 INVALID_TYPE
3. 若 Status ≠ 2 → 400 INVALID_STATUS
4. 事务内：
   ├─ ① 将原记录 DormBooking.Status = 3（已退房）
   ├─ ② 创建新记录：
   │    ├─ Type = 2（退房）
   │    ├─ BookingDate = today()
   │    ├─ Status = 3（已退房）
   │    ├─ EmployeeId / EmployeeCode / EmployeeName / DormCode 等字段复制自原记录
   │    ├─ Registrar = 当前登录用户名
   │    ├─ RegistrationDate = now()
   │    └─ CreatedAt = now()
   ├─ ③ 联动：SysEmployee.DormCode = NULL（员工变为未分配宿舍）
5. 返回 ApiResponse { success: true, data: { originalRecord, newRecord } }
```

> **与现有 `/api/v1/bookings/{id}/check-out` 的区别**：
> - **check-out**（既有）：走 4 步表单，需选员工+选在宿记录+输入退房日期；
> - **confirm-checkout-create**（v2.11.17）：列表页"退房"按钮直接调用，弹出确认对话框后**自动创建一条 Type=2 退房的新记录**，无需手动选日期。
>
> **与 v2.11.12 confirm-checkout 的区别**：
> - **confirm-checkout**（v2.11.12）：针对 Type=2 退房 && Status=1 预约的记录，一键确认即变更为已退房（**不创建新记录**）；
> - **confirm-checkout-create**（v2.11.17）：针对 Type=1 入住 && Status=2 在宿的记录，确认后**创建一条新的 Type=2 退房记录**并变更原记录状态为已退房。

---

## 5. Web 页面

| 页面 | 路径 | 功能 |
|------|------|------|
| 办理登记列表 | /Booking | 模糊查询 + 列表 + 操作（**修改（仅预约）/ 入住（仅预约）/ 撤销（v2.11.10：今日已退房）/ 退房（v2.11.17：在宿 → 创建退房新记录）/ 撤销（v2.11.17：今日在宿 → 取消）/ 撤销预约（v2.11.11，最右侧）/ 删除**） |
| 办理入住（弹窗） | Modal（#checkInModal） | 选员工 → 选房间 → 选日期 → 保存（v2.11.16：从独立页面改为独占弹窗） |
| 办理退房（弹窗） | Modal（#checkOutModal） | 选员工 → 选在宿记录 → 选日期 → 保存（v2.11.16：从独立页面改为独占弹窗） |
| 修改记录 | /Booking/Edit/{id} | 修改日期/原因/备注（v2.11.16：新增员工基本信息只读展示区） |

---

## 6. 交互流程

### 6.1 办理入住（弹窗模式，v2.12.34 升级）

> **v2.11.16 变更**：从独立页面 `/Booking/CheckIn` 改为 Bootstrap Modal 独占弹窗（#checkInModal），弹窗宽度 xl（1140px），背景半透明遮罩。
>
> **v2.12.34 变更**：
> 1. **校验信息移至"操作类型"右侧同行**——区块 3 采用 `d-flex` 同行两栏：左栏操作类型固定 100px，右栏校验信息 flex:1；
> 2. **校验失败锁定**——员工当前已在宿时，禁用房间下拉 + 禁用提交按钮（`lockActionIn(true)`）；
> 3. **入住日期 > 当前日期时操作类型二选一**——入住日期选择未来日期时，操作类型区域显示"入住"/"预约"两个 radio 按钮（2 选 1）；入住日期 ≤ 当前日期时仅显示"入住"Badge；
> 4. **预分配房号**——校验通过后，按优先级自动推荐房间：①同班次 ②入住率高+有余量 ③同部门；推荐的同时自动选中下拉框；
> 5. **校验信息格式**——校验失败：`✗ 入住校验：校验失败！入住日期 2026-08-28 ，员工当前已在宿 D-178（房号加大红色字体加粗），请先办理退房`；校验通过：`✓ 入住校验：校验通过！员工状态正常，可办理入住`

```
[弹窗布局]
┌─────────────────────────────────────────────────────┐
│  办理入住                                             │
├─────────────────────────────────────────────────────┤
│  [区块 1：员工选择]                                   │
│  输入框：姓名/工号/手机号模糊筛选                     │
│  选中后显示：工号/姓名/手机/部门/类型/班次            │
├─────────────────────────────────────────────────────┤
│  [区块 2：入住信息]                                   │
│  入住日期 * | 原因 | 登记人                           │
│  [date picker]  [入职/调岗/...]  [admin-只读]        │
├─────────────────────────────────────────────────────┤
│  [区块 3：操作类型与校验（同行两栏）] ← v2.12.34 新增 │
│  ┌──────────┬──────────────────────────────────────┐ │
│  │操作类型  │校验信息                              │ │
│  │(固定100px│ ✗ 入住校验：校验失败！入住日期...      │ │
│  │  或入住/ │   员工当前已在宿 D-178，请先办理退房  │ │
│  │  预约)   │                                      │ │
│  │          │ ✓ 入住校验：校验通过！员工状态正常... │ │
│  └──────────┴──────────────────────────────────────┘ │
├─────────────────────────────────────────────────────┤
│  [区块 4：选择房间]                                   │
│  下拉：房号 + 楼栋 + 地址 + 余量                      │
│  ℹ 默认预分配房号：D-088（同班次）· 床位 2           │
├─────────────────────────────────────────────────────┤
│  [取消]  [提交入住]（校验失败时禁用）                  │
└─────────────────────────────────────────────────────┘
```

**办理入住交互流程（v2.12.34 升级后）**：

```
[步骤 1] 选择员工
   ├─ 输入框：模糊查询（姓名/工号/手机号）
   ├─ 下拉建议：返回前 10 条匹配员工
   └─ 选中后自动填入：手机号、部门、入职日期、班次

[步骤 2] 选择入住日期（默认 = 今天）
   ├─ 入住日期 > 当前日期 → 操作类型显示"入住"/"预约"二选一
   ├─ 入住日期 ≤ 当前日期 → 操作类型仅显示"入住"Badge
   └─ 校验：≥ 入职日期

[步骤 3] 员工校验（选择员工后即刻执行）
   ├─ 员工当前在宿 → 校验失败，锁定房间选择 + 提交按钮
   │   提示：✗ 入住校验：校验失败！入住日期 XXXX ，员工当前已在宿 D-XXX，请先办理退房
   └─ 员工当前不在宿 → 校验通过，解锁房间选择 + 提交按钮
       提示：✓ 入住校验：校验通过！员工状态正常，可办理入住

[步骤 4] 选择房间（校验通过后解锁）
   ├─ 下拉列表：仅显示「启用 + 余量 > 0 + 日期不冲突」的房间
   ├─ 默认预分配：按优先级自动推荐（1.同班次 → 2.入住率高+余量 → 3.同部门）
   └─ 每项显示：房号 + 楼栋 + 地址 + 余量（"2 / 4 人"）

[步骤 5] 提交
   ├─ 登记日期：当前时间（自动）
   ├─ 登记人：当前登录用户名（自动）
   └─ 原因：可选（入职/调岗等）
```

### 6.2 办理退房（3 步表单，v2.11.16 改为弹窗模式）

> **v2.11.16 变更**：从独立页面 `/Booking/CheckOut` 改为 Bootstrap Modal 独占弹窗（#checkOutModal），弹窗宽度 xl（1140px），背景半透明遮罩。

```
[步骤 1] 选择员工
   └─ 同上，限定为「当前在宿」员工

[步骤 2] 选择在宿记录
   └─ 自动列出该员工的所有在宿记录（含房间、入住日期）

[步骤 3] 选择退房日期
   ├─ 默认 = 今天
   └─ 校验：≥ 入住日期 且 ≤ 下一次入住日期 - 1（如有）

[步骤 4] 提交
   └─ 同上
```

### 6.3 列表查询

```
[筛选区]
  ├─ 关键词：员工姓名/工号/手机号
  ├─ 类型：入住/退房/全部
  ├─ 状态：预约/在宿/已退房/已取消/全部
  ├─ 日期范围：从 ~ 至
  └─ [查询] [重置] [+ 办理入住] [+ 办理退房]

[列表区]
  序号 | 工号 | 姓名 | 部门 | 宿舍 | 类型 | 入退日期 | 状态 | 登记人 | 登记时间 | 操作
  操作列：**修改（仅 Status=1 预约可点） | 撤销（v2.11.10：仅 BookingDate=今天 && Status=3 已退房） | 入住（仅 Type=1 入住 && Status=1 预约） | 退房（v2.11.17：Type=1 入住 && Status=2 在宿 → 弹出确认创建退房新记录；或 Type=2 退房 && Status=1 预约 → 快速确认已退房） | 撤销（v2.11.17：仅 BookingDate=今天 && Type=1 入住 && Status=2 在宿 → 取消为已取消） | 撤销预约（v2.11.11：仅 Status=1 预约 → 取消为已取消，位置在最右侧）**
```

> **v2.11.11 操作列新增"撤销"按钮（撤销预约）**：当记录的 **Status = 1 预约** 时，操作列**最右侧**显示红色 `btn-outline-danger` "**撤销**"按钮。点击调用新接口 `POST /api/v1/bookings/{id}/cancel-reservation`，服务端将 **Status 即时变更为 4 已取消**，同时：
> - `Registrar`（登记人）更新为当前登录账号
> - `RegistrationDate`（登记时间）更新为服务端 `now()`
> - `UpdatedAt` 更新为服务端 `now()`
> 
> 业务含义：HR 对未来预约入住的记录进行"主动取消"操作，无需校验房间余量（从未入住）。
> 
> **v2.11.12 操作列"退房"按钮显示条件扩展**：当记录的 **Type = 2 退房** 且 **Status = 1 预约** 时，操作列在"修改"与"撤销预约"之间显示黄色 `btn-outline-warning` "**退房**"按钮。点击调用新接口 `POST /api/v1/bookings/{id}/confirm-checkout`，服务端将 **Status 即时变更为 3 已退房**，同时：
> - `Registrar`（登记人）更新为当前登录账号
> - `RegistrationDate`（登记时间）更新为服务端 `now()`
> - `UpdatedAt` 更新为服务端 `now()`
> - 联动：`SysEmployee.DormCode = NULL`（员工变为未分配宿舍）
> 
> 业务含义：HR 对未来预约退房的记录进行"快速确认"操作，无需走 `/Booking/CheckOut` 4 步表单。
> 
> **v2.11.13 操作按钮点击后即时刷新规则**：点击"退房"/"入住"/"撤销"任一操作按钮，服务端处理成功后，**前端必须即时刷新列表并显示当前记录的最新数据**（状态徽章、登记人、登记时间、操作按钮按新状态重新计算）：
> - **Razor 实现**：使用 `fetch` 异步调用接口，`result.success` 后调用 `location.reload()` 或 `刷新列表 AJAX`；当前实现采用 `location.reload()`（简单可靠，整页刷新）
> - **原型实现**：使用 `registerList.reset()` 重新拉取数据 + 重新渲染；或 `render()` 函数触发列表重绘
> - **刷新范围**：至少刷新当前记录所在行；推荐刷新整个列表（保证筛选条件、分页、合计数等同步）
> - **刷新时机**：在 `result.success` 后立即触发；不应在 `alert('xxx成功')` 后再延迟刷新（避免用户感知卡顿）
> - **错误处理**：若 `result.success = false`，**不刷新**列表，仅弹窗显示错误信息
> 
> **最终操作列按钮顺序**（完整版）：**修改 → 撤销退房(v2.11.10) → 入住(v2.11.8) → 退房(v2.11.12) → 撤销预约(v2.11.11)**，每个按钮在不同状态下互斥显示。
> 
> **与 v2.11.10 撤销退房按钮的区分**（同样文字"撤销"但语义不同）：
> | 维度 | v2.11.10 撤销退房 | v2.11.11 撤销预约 |
> |------|---------------|---------------|
> | 触发条件 | BookingDate==今天 && Status=3 已退房 | Status=1 预约 |
> | 位置 | "修改"按钮**右侧**（中间位置） | 操作列**最右侧** |
> | 颜色 | 青色 `btn-outline-info` | 红色 `btn-outline-danger` |
> | 目标状态变更 | 3 已退房 → 2 在宿 | 1 预约 → 4 已取消 |
> | 是否校验房间余量 | ✅ 是 | ❌ 否（从未入住） |
> | 业务场景 | 误办了当天的退房，恢复在宿状态 | 主动取消未来的预约登记 |
> 
> **v2.11.10 操作列新增"撤销"按钮**（撤销退房）：当记录的 **入退日期 = 服务端当天日期**（`BookingDate == today()`）且 **Status = 3 已退房** 时，操作列显示青色 `btn-outline-info` "**撤销**"按钮，位置在"修改"按钮右侧。点击调用新接口 `POST /api/v1/bookings/{id}/undo-checkout`，**服务端先校验该入住的宿舍房号是否还有余量床位**：
> - **有余量** → 服务端将 Status 从 3 已退房即时变更为 2 在宿，并同步 `Registrar=当前登录账号`、`RegistrationDate=now()`、`UpdatedAt=now()`，联动恢复 `SysEmployee.DormCode`
> - **无余量** → 返回 400 NO_CAPACITY 错误，前端弹窗提示用户：**"床位已满，撤销退房失败！"**（不修改任何字段）
> 
> 业务含义：HR 误办了当天的退房后，可一键"撤销退房"恢复到在宿状态；若床位已被新员工入住则不允许撤销。
> 
> **v2.11.9 操作列按钮顺序调整**："入住"按钮位置由原"修改"左侧调整为"修改"右侧，最终从左至右排列：**修改 → 撤销退房(v2.11.10) → 入住 → 退房 → 撤销预约(v2.11.11)**（两个"撤销"按钮在不同状态下互斥显示）。

> **v2.11.17 操作列变更**：
>
> ### v2.11.17.a "退房"按钮行为变更
>
> 当记录的 **Type=1 入住 && Status=2 在宿** 时，操作列显示黄色 `btn-outline-warning` "**退房**"按钮（位置在"入住"与"撤销"之间）。点击后：
> 1. **弹出一个确认对话框**，展示当前日期（服务端 today）和类型为"退房"的信息；
> 2. 用户点击"确认" → 调用新接口 `POST /api/v1/bookings/{id}/confirm-checkout-create`：
>    - 创建一条新的 Type=2 退房类型的 DormBooking 记录（BookingDate=今天）；
>    - 将原入住记录 Status 变更为 3 已退房；
>    - 联动 `SysEmployee.DormCode = NULL`（员工变为未分配宿舍）；
>    - 新记录同步 `Registrar=当前登录账号`、`RegistrationDate=now()`；
> 3. 用户点击"取消" → 返回，不做任何操作；
> 4. 成功后前端即时刷新列表（v2.11.13 规则）。
>
> > **与 v2.11.12 退房按钮的融合**：
> > - v2.11.12 退房按钮：Type=2 退房 && Status=1 预约 → 快速确认已退房（**不创建新记录**）；
> > - v2.11.17 退房按钮：Type=1 入住 && Status=2 在宿 → 弹出确认后**创建 Type=2 退房新记录**（**变更原记录状态**）；
> > - 两者共用同一个"退房"按钮文本和颜色，但根据记录状态调用不同接口。
>
> ### v2.11.17.b 操作列新增"撤销"按钮（在宿场景）
>
> 当记录的 **Type=1 入住 && Status=2 在宿 && BookingDate == 今天（服务端日期）** 时，操作列在"退房"按钮右侧显示红色 `btn-outline-danger` "**撤销**"按钮。点击后：
> 1. 弹出确认对话框："确认撤销该入住记录？（状态：在宿 → 已取消）"；
> 2. 用户确认 → 调用新接口 `POST /api/v1/bookings/{id}/cancel-today`：
>    - 将原记录 Status 即时变更为 4 已取消；
>    - 同步 `Registrar=当前登录账号`、`RegistrationDate=now()`、`UpdatedAt=now()`；
>    - **不创建新记录**，**不联动 SysEmployee.DormCode**（原入住记录仍在）；
> 3. 用户取消 → 返回，不做任何操作；
> 4. 成功后前端即时刷新列表（v2.11.13 规则）。
>
> > **与 v2.11.11 撤销预约按钮的区分**（同样文字"撤销"但语义不同）：
> > | 维度 | v2.11.10 撤销退房 | v2.11.11 撤销预约 | v2.11.17 撤销在宿 |
> > |------|---------------|---------------|---------------|
> > | 触发条件 | BookingDate==今天 && Status=3 已退房 | Status=1 预约 | BookingDate==今天 && Type=1 && Status=2 在宿 |
> > | 位置 | "修改"右侧 | 操作列最右侧 | "退房"右侧 |
> > | 颜色 | 青色 `btn-outline-info` | 红色 `btn-outline-danger` | 红色 `btn-outline-danger` |
> > | 目标状态变更 | 3 已退房 → 2 在宿 | 1 预约 → 4 已取消 | 2 在宿 → 4 已取消 |
> > | 是否创建新记录 | ❌ 否 | ❌ 否 | ❌ 否 |
> > | 是否校验房间余量 | ✅ 是 | ❌ 否 | ❌ 否 |
> > | 联动 SysEmployee.DormCode | ✅ 恢复 | ❌ 不联动 | ❌ 不联动 |
> > | 业务场景 | 误办当天退房，恢复在宿 | 主动取消未来预约 | 当天入住后发现信息有误，撤销入住 |
>
> **v2.11.17 最终操作列按钮顺序**（完整版）：**修改 → 撤销退房(v2.11.10) → 入住(v2.11.8) → 退房(v2.11.17) → 撤销在宿(v2.11.17) → 撤销预约(v2.11.11)**，每个按钮在不同状态下互斥显示。

> **v2.11.8 操作列新增"入住"快速确认按钮**：当记录的 **Type=1 入住** 且 **Status=1 预约** 时，操作列显示绿色 `btn-outline-success` "**入住**"按钮。点击后即时调用新接口 `POST /api/v1/bookings/{id}/confirm-checkin`，**服务端先校验该入住的宿舍房号是否还有余量床位**：
> - **有余量** → 服务端将状态从"预约"即时变更为"在宿"，同时：`Registrar`（登记人）更新为当前登录账号；`RegistrationDate`（登记时间）更新为服务端 `now()`；`UpdatedAt` 更新为服务端 `now()`
> - **无余量** → 返回 400 NO_CAPACITY 错误，前端弹窗提示用户：**"床位已满，请更换其他房间"**（v2.11.8.b 优化文案）
> 
> 业务含义：将"未来预约入住"快速确认为"当前在宿"，省去重复走 `/Booking/CheckIn` 流程，同时确保床位余量校验不被绕过。  
> 
> **v2.11.7 操作列权限规则**：① "**修改**"按钮（v2.11.7 起，原"编辑"更名为"修改"）**仅当 Status=1 预约**时启用，其他状态（2 在宿 / 3 已退房 / 4 已取消）一律**只读**（disabled 灰色 + 无图标，**仅显示"修改"文本**，鼠标悬停提示"仅预约状态可修改"）；② "退房"按钮仅 Status=2 在宿时显示；③ "删除"按钮仅 Status=1/4 时显示（预约可删，已取消可删，在宿/已退房不可删）；④ "**入住**"按钮（v2.11.8 新增）仅 Type=1 && Status=1 时显示。

---

## 7. 校验规则详解

### 7.1 入住校验（选择员工后即刻执行，v2.12.34 升级）

> **v2.12.34 变更**：校验信息从右侧独立面板移至"操作类型"右侧同行显示；校验失败时锁定房间选择和提交按钮；入住日期 > 当前日期时操作类型显示"入住"/"预约"二选一；校验通过后按优先级预分配房号。

```
1. 取该员工的最后一条办理登记（按 BookingDate DESC）
2. 若最后一条为「在宿」→ 校验失败，锁定房间选择 + 提交按钮
   提示：✗ 入住校验：校验失败！入住日期 {date} ，员工当前已在宿 {dormCode}（房号加大红色字体加粗），请先办理退房
3. 若最后一条为「退房」且 BookingDate ≥ 本次入住日期 → 报错「请检查日期顺序」
4. 若本次入住日期 < 入职日期 → 报错「入住日期不能早于入职日期」
5. 入住日期 > 当前日期 → 操作类型区域显示"入住"/"预约"二选一（radio 按钮）
6. 入住日期 ≤ 当前日期 → 操作类型区域仅显示"入住"Badge
7. 校验通过后：按优先级预分配房号
   优先级 1：同班次（同 attendanceTypeId 且有余量）
   优先级 2：入住率高且有余量（available/capacity ≥ 0.5）
   优先级 3：同部门（同 departmentId 且有余量）
   推荐同时自动选中下拉框，并显示床位号（activeCount + 1）
8. 房间余量校验：capacity - currentIn宿 - 预约入住 ≥ 1
9. 日期重叠校验：该房间内不能有同日期的其他入住/退房预约
```

**预分配房号规则（v2.12.34 新增）**：

| 优先级 | 规则 | 说明 |
|--------|------|------|
| 1 | 同班次 | 该宿舍当前在宿员工中有与所选员工相同考勤班次的 |
| 2 | 入住率高 + 余量 | 剩余容量 / 总容量 ≤ 0.5 且有剩余容量的宿舍 |
| 3 | 同部门 | 该宿舍当前在宿员工中有与所选员工相同部门的 |
| 兜底 | 首个可用 | 以上都不满足时，取第一个有余量的房间 |

**床位号计算**：
```
床位号 = 该宿舍当前在宿人数 + 1
存储格式：逗号分隔的整数列表（如 "1,2,3,4"）
```

### 7.2 退房校验（提交时执行）

```
1. 记录必须存在且状态为「在宿」
2. 取该员工的下一条办理登记（按 BookingDate ASC）
3. 若下一条为「入住」且 BookingDate ≤ 本次退房日期 → 报错「不能晚于下次入住」
4. 退房日期 ≤ 今天
```

---

## 8. 验收清单

- [ ] 列表页支持按姓名/工号/手机号模糊查询
- [ ] 选择员工后自动显示手机号 + 部门
- [ ] **入住校验信息移至"操作类型"右侧同行显示（v2.12.34）**
- [ ] **校验失败时锁定房间选择和提交按钮（v2.12.34）**
- [ ] **入住日期 > 当前日期时操作类型显示"入住"/"预约"二选一（v2.12.34）**
- [ ] **校验通过后按优先级预分配房号（v2.12.34）**
- [ ] 房间下拉只显示有余量 + 日期不冲突的房间
- [ ] 入住日期默认 = 今天
- [ ] 退房日期默认 = 今天
- [ ] 登记日期自动 = 当前时间
- [ ] 登记人自动 = 当前登录用户名
- [ ] 同员工同日不能既有入住又有退房
- [ ] 入住日期不能早于入职日期
- [ ] 退房日期不能晚于今天
- [ ] 退房日期不能晚于下一次入住日期
- [ ] 仅「预约/已取消」状态可删除
- [ ] 列表显示当前所有员工最近的办理登记

---

## 9. 与现有模块的关系

- **人员清单**：`SysEmployee.DormCode` 字段保留冗余（加速查询），但以最近一条「在宿」记录为准
- **宿舍档案**：`CurrentResidentCount` 实时查询 DormBooking（Status=在宿）
- **费用分摊**：DormBilling.ResidentCount 改为查询 DormBooking（Status=在宿）

---

## 10. Mock 数据完整性说明（v2.11.2 增补）

### 10.1 数据规模

| 统计项 | 数量 |
|--------|------|
| 人员总数 | 650 人 |
| 办理登记总数 | 750 条 |
| 预约（status=1） | 1,455 条 |
| 在宿（status=2） | 716 条 |
| 已退房（status=3） | 156 条 |
| 已取消（status=4） | 3 条 |

### 10.2 状态分布说明

| 状态值 | 含义 | 说明 |
|--------|------|------|
| 1 | 预约 | 预登记尚未实际入住，如预约入职 |
| 2 | 在宿 | 当前正在宿舍居住 |
| 3 | 已退房 | 历史记录，已完成退房 |
| 4 | 已取消 | 预约被取消，不计入在住 |

### 10.3 典型场景数据

| 员工 | 场景 | 记录数 | 用途 |
|------|------|--------|------|
| 林涛（EMP-2026-080） | 多次调宿历史 | 5 条 | 展示完整一进一出生命周期 |
| 吴芳（EMP-2026-050） | 预约取消 | 1 条 | 展示 status=4 场景 |
| 王强（EMP-2026-090） | 未来预约 | 1 条 | 展示预约入住场景 |

### 10.4 一进一出校验示例（林涛）

```
2023-01-15 入住 D-030 → 2024-06-30 退房 D-030（离职）
2025-03-01 入住 D-055 → 2026-02-15 退房 D-055（调宿）
2026-02-16 入住 D-088 → 当前在宿
```

### 10.5 与其他表的关系

| 表 | 关联字段 | 关系说明 |
|----|----------|----------|
| PERSONNEL | employeeId → status | 员工在职状态，独立于办理登记 |
| RESIDENCIES | dormCode, employeeId | 实际在住统计以此为准 |
| DORMS | dormCode | 宿舍档案，容量信息 |
| BOOKINGS | — | 本表，记录全生命周期 |
---

## 11. 原型开发注意事项（v2.11.4 增补）

### 11.1 JavaScript 变量名冲突问题

> ⚠️ **重要**：原型页面 `booking/index.html` 中使用的 JavaScript 变量必须避免与 `mock-data.js` 中的变量名冲突。

#### 已冲突的变量名（禁止使用）

| 变量名 | mock-data.js 中的定义 | 冲突后果 |
|--------|----------------------|---------|
| `STATUS_MAP` | 员工在职状态映射 | SyntaxError: Identifier 'STATUS_MAP' has already been declared |
| `STATUS_BADGE` | 员工状态徽章样式 | 同上 |
| `BOOKING_STATUSES` | 办理状态映射 | 同上 |
| `BOOKING_STATUS_BADGE` | 办理状态徽章样式 | 同上 |

#### 正确的变量命名

```javascript
// ✅ 办理登记页面使用 BK_ 前缀
var BK_STATUS_TEXT = {1:"预约", 2:"在宿", 3:"已退房", 4:"已取消"};
var BK_STATUS_CLASS = {1:"bg-warning text-dark", 2:"bg-success", ...};

// ✅ 页面初始化使用 initPage 模式
function initPage() {
    if (document.readyState === 'loading' || typeof BOOKINGS === 'undefined') {
        setTimeout(initPage, 50);
        return;
    }
    drawList();
}
initPage();
```

### 11.2 页面初始化时序问题

页面 JavaScript 在 `mock-data.js` 加载完成前执行会导致 `BOOKINGS is not defined` 错误。

**正确做法**：
1. 使用 `initPage()` 模式延迟执行
2. 在函数内部检查 `typeof BOOKINGS === 'undefined'`
3. 等待数据加载完成后再调用 `drawList()`

**错误做法**：
```javascript
// ❌ 直接调用 drawList() 可能导致 BOOKINGS 未定义
drawList();  // 如果 mock-data.js 还在加载中会报错
```

---

## 13. 修改办理登记页面 — 字段数据来源与关系矩阵（v2.11.5 增补）

> **页面入口**：`/Booking/Edit/{id}`（原型：`04-HTML原型/booking/edit.html`）
> **数据载体**：`DormBooking` 表（按 Id 查询单条记录）
> **作用**：在不改变类型/员工/宿舍的前提下，修正入退日期、原因、备注、状态（仅预约可改）

### 13.0 页面布局（v2.11.15.d 重构 → v2.11.16 增补）

> **变更背景**：原 edit.html 将数据关联可视化（ER 图/字段矩阵/字典/校验/写入流程）混入用户表单页面，导致页面结构混乱、大量开发元数据暴露给用户。v2.11.15.d 参照"编辑人员"页面的标准表单布局进行全面重构。

**v2.11.16 增补**：修改办理登记页面新增"员工基本信息只读展示区"，展示员工完整信息（工号、姓名、部门、员工类型、考勤班次、手机、入职日期、在职状态），数据值与记录对应项一致（非空值初始状态）。

**新版页面结构（v2.11.16 增补后）**：

```
┌──────────────────────────────────────────────────────┐
│  页面标题：修改办理登记    [← 返回列表]               │
├──────────────────────────────────────────────────────┤
│  [信息摘要卡片（只读）]                               │
│  员工：张三（EMP-2026-001）                          │
│  宿舍：D-001  类型：[入住]  状态：[预约]             │
│  登记人：admin（2026-07-12 09:30）                   │
├──────────────────────────────────────────────────────┤
│  [员工基本信息卡片（只读灰底）]  ← v2.11.16 新增      │
│  工号：EMP-2026-001  姓名：张三  部门：生产部         │
│  员工类型：合同工  考勤班次：早班  手机：138xxxx       │
│  入职日期：2026-01-01  在职状态：在职                 │
├──────────────────────────────────────────────────────┤
│  [表单卡片]                                          │
│  ┌──────────────────┬──────────────────┐              │
│  │ 入退日期 *        │ 原因             │              │
│  │ [date picker]    │ [入职/调岗/...]  │              │
│  └──────────────────┴──────────────────┘              │
│  ┌──────────────────┐                                 │
│  │ 状态 *（仅预约显示）│                                 │
│  │ [预约/在宿/...]   │                                 │
│  └──────────────────┘                                 │
│  ┌──────────────────────────────────────────┐         │
│  │ 备注（500 字上限，实时字数统计）            │         │
│  │ [textarea rows=2]              0 / 500   │         │
│  └──────────────────────────────────────────┘         │
│  ───────────────────────────────────────────          │
│  [系统自动记录（只读灰底）]                             │
│  登记日期：2026-07-12 09:30  登记人：admin             │
│  ───────────────────────────────────────────          │
│                    [取消]  [保存修改]                   │
└──────────────────────────────────────────────────────┘
```

**v2.11.16 员工基本信息数据来源**：

| 字段 | 数据源 | 关联路径 |
|------|--------|----------|
| 工号 | SysEmployee.EmployeeCode | DormBooking.EmployeeId → SysEmployee.Id |
| 姓名 | SysEmployee.RealName | 同上 |
| 部门 | SysEmployee.Department | 同上（冗余字段） |
| 员工类型 | SysEmployee.EmployeeType.Name | 同上 → EmployeeType 表 JOIN |
| 考勤班次 | SysEmployee.AttendanceType.Name | 同上 → AttendanceType 表 JOIN |
| 手机 | SysEmployee.Phone | 同上 |
| 入职日期 | SysEmployee.HireDate | 同上 |
| 在职状态 | SysEmployee.EmploymentStatus.Name | 同上 → EmploymentStatus 表 JOIN |

**重构前后对比**：

| 维度 | 旧版（v2.11.5.b 及以前） | **新版（v2.11.15.d）** |
|------|------------------------|---------------------|
| 布局结构 | 左表单 + 右数据关联可视化面板（5 个 Tab） | 顶部只读信息摘要 + 标准表单 + 底部操作按钮 |
| 只读字段 | 自定义 `info-item` flex 布局 + 数据源徽章 | 统一 `.info-summary` 卡片 + 标准标签值对 |
| 可编辑字段 | 表单内嵌字段元数据提示（校验码/字典/源表） | 标准 `<label>` + `<small class="text-muted">` 辅助说明 |
| 系统记录 | 黄色 `auto-record` 卡片 | 标准只读 `<input readonly disabled>` |
| 操作按钮 | 嵌入表单底部 | 独立 `<hr>` 分隔，右对齐 |
| 数据关联可视化 | 右侧 5 Tab（字段矩阵/ER 图/字典/校验/写入流程） | **移除**（属于开发文档，不应出现在用户页面） |

**新版页面结构**：

```
┌──────────────────────────────────────────────────────┐
│  页面标题：修改办理登记    [← 返回列表]               │
├──────────────────────────────────────────────────────┤
│  [信息摘要卡片（只读）]                               │
│  员工：张三（EMP-2026-001）                          │
│  宿舍：D-001                                         │
│  类型：[入住]  状态：[预约]  考勤班次：[早班]         │
├──────────────────────────────────────────────────────┤
│  [表单卡片]                                          │
│  ┌──────────────────┬──────────────────┐              │
│  │ 入退日期 *        │ 原因             │              │
│  │ [date picker]    │ [入职/调岗/...]  │              │
│  └──────────────────┴──────────────────┘              │
│  ┌──────────────────┐                                 │
│  │ 状态 *（仅预约显示）│                                 │
│  │ [预约/在宿/...]   │                                 │
│  └──────────────────┘                                 │
│  ┌──────────────────────────────────────────┐         │
│  │ 备注（500 字上限，实时字数统计）            │         │
│  │ [textarea rows=2]              0 / 500   │         │
│  └──────────────────────────────────────────┘         │
│  ───────────────────────────────────────────          │
│  [系统自动记录（只读灰底）]                             │
│  登记日期：2026-07-12 09:30  登记人：admin             │
│  ───────────────────────────────────────────          │
│                    [取消]  [保存修改]                   │
└──────────────────────────────────────────────────────┘
```

### 13.1 页面字段全览

| # | UI 字段 | HTML id | 可编辑 | 数据源表 | 源字段 | 类型/枚举 | 渲染方式 |
|---|--------|---------|--------|----------|--------|----------|----------|
| 1 | 员工 | `employeeName` | ❌ 只读 | `DormBooking` | `EmployeeCode` + `EmployeeName`（冗余） | string | `{姓名}（{工号}）` |
| 2 | 考勤班次 | `empAtt` | ❌ 只读 | `SysEmployee` | `AttendanceTypeId` → `AttendanceType.Name`（v2.11.7 FK 关联） | INT FK | Badge（按 ATTENDANCE_BADGE 配色） |
| 3 | 宿舍 | `dormCode` | ❌ 只读 | `DormBooking` | `DormCode` | string | 原样显示 |
| 4 | 类型 | `type` | ❌ 只读 | `DormBooking` | `Type` | TINYINT | 1=入住 / 2=退房（BOOKING_TYPES） |
| 5 | 状态 | `status` / `statusWrap` | ⚠️ 条件可编辑 | `DormBooking` | `Status` | TINYINT | 1=预约 2=在宿 3=已退房 4=已取消 |
| 6 | 入退日期 | `bookingDate` | ✅ 必填可编辑 | `DormBooking` | `BookingDate` | DATE | `<input type="date">` |
| 7 | 原因 | `reason` | ✅ 可编辑 | `DormBooking` | `Reason` | string（字典枚举） | `<select>` 下拉 |
| 8 | 备注 | `remark` | ✅ 可编辑 | `DormBooking` | `Remark` | string(≤500) | `<textarea rows="3">` |
| 9 | 登记日期 | `regDate` | ❌ 只读（不可改） | `DormBooking` | `RegistrationDate`（默认 `now()`） | DATETIME | `yyyy-MM-dd HH:mm` |
| 10 | 登记人 | `registrar` | ❌ 只读（不可改） | `DormBooking` | `Registrar`（默认登录用户名） | string(≤50) | 文本 |

### 13.2 字段-数据源关系矩阵

| UI 字段 | 直接存储 | 关联表 | 关联字段 | 关联路径 | 引用数据流向 |
|---------|----------|--------|----------|----------|-------------|
| 员工（工号+姓名） | ✅ DormBooking 冗余 | SysEmployee | `EmployeeCode`, `RealName` | `DormBooking.EmployeeId` → `SysEmployee.Id` | UI 显示 DormBooking 冗余字段（避免 N+1） |
| 考勤班次 | ❌ 不存储 | SysEmployee → AttendanceType | `AttendanceTypeId` → `AttendanceType.Id` → `Name` | `DormBooking.EmployeeId` → `SysEmployee.AttendanceTypeId` → `AttendanceType.Name` | **运行时 JOIN**，单一数据源在 `AttendanceType` 字典表 |
| 宿舍 | ✅ DormBooking 冗余 | Dorm | `DormCode` | `DormBooking.DormCode` → `Dorm.DormCode` | UI 直接显示 DormBooking 冗余字段 |
| 类型 | ✅ DormBooking | — | — | 直接读取 `DormBooking.Type` | 通过 `BOOKING_TYPES` 字典翻译 |
| 状态 | ✅ DormBooking | — | — | 直接读取 `DormBooking.Status` | 通过 `BOOKING_STATUSES` 字典翻译 + Badge 配色 |
| 入退日期 | ✅ DormBooking | — | — | 直接读取 `DormBooking.BookingDate` | — |
| 原因 | ✅ DormBooking | — | — | 直接读取 `DormBooking.Reason` | 前端 `<select>` 硬编码枚举（入职/调岗/换房/预约/离职/其他） |
| 备注 | ✅ DormBooking | — | — | 直接读取 `DormBooking.Remark` | — |
| 登记日期 | ✅ DormBooking | — | — | 直接读取 `DormBooking.RegistrationDate` | 创建时由后端写入 `now()`，**不可修改** |
| 登记人 | ✅ DormBooking | SysUser | `UserName` | `DormBooking.Registrar` ↔ 当前登录上下文 | 创建时由后端写入当前用户名，**不可修改** |

### 13.3 关系图（ER 视图）

```
┌──────────────────┐         ┌──────────────────┐         ┌──────────────────┐
│   SysUser        │         │   SysEmployee    │         │  AttendanceType  │
│ (登录用户)        │         │   (员工主表)      │         │   (基础字典)      │
├──────────────────┤         ├──────────────────┤         ├──────────────────┤
│ Id (PK)          │         │ Id (PK)          │◄────────│ Id (PK)          │
│ UserName ─────────┼─写入────┼─► EmployeeId (FK)│         │ Code (DEFAULT..) │
└──────────────────┘         │ EmployeeCode     │         │ Name (默认/早班..)│
       │                     │ RealName         │         │ WorkHours        │
       │ 当前登录            │ DepartmentId     │         └──────────────────┘
       ▼                     │ Department       │                ▲
┌──────────────────┐         │ EmployeeTypeId   │                │ FK (v2.11.7)
│  DormBooking     │         │ Phone            │                │
│  (办理登记表)     │         │ Status           │         ┌──────────────────┐
│  【本页编辑对象】 │         │ HireDate         │         │   Department     │
├──────────────────┤         │ DormCode         │         │  (部门字典)       │
│ Id (PK)          │         │ AttendanceTypeId─┼─────────┤ Id (PK)          │
│ EmployeeId (FK)──┼────────►│                  │         │ Code / Name      │
│ EmployeeCode(冗余)│        │ ...              │         └──────────────────┘
│ EmployeeName(冗余)│        └──────────────────┘
│ Phone (冗余)     │                ▲
│ Department(冗余) │                │ FK
│ DormCode (FK)────┼────────►┌──────────────────┐
│ Type             │         │      Dorm        │
│ BookingDate ◄────┼─ 编辑   │   (宿舍档案)      │
│ Status ◄─────────┼─ 条件编辑├──────────────────┤
│ Reason ◄─────────┼─ 编辑   │ DormCode (PK)    │
│ Remark ◄─────────┼─ 编辑   │ Building / Floor │
│ RegistrationDate │         │ Capacity / Status│
│ Registrar        │         │ DepartmentId     │
│ CreatedAt/Updat..│         └──────────────────┘
└──────────────────┘
```

### 13.4 字段可编辑性矩阵（按状态分流）

| UI 字段 | 状态=1 预约 | 状态=2 在宿 | 状态=3 已退房 | 状态=4 已取消 |
|---------|------------|------------|--------------|--------------|
| 员工 | 只读 | 只读 | 只读 | 只读 |
| 考勤班次 | 只读（Badge） | 只读 | 只读 | 只读 |
| 宿舍 | 只读 | 只读 | 只读 | 只读 |
| 类型 | 只读 | 只读 | 只读 | 只读 |
| **状态** | ✅ **下拉可选**：预约 / 对应类型状态（在宿或已退房）/ 已取消 | 只读 | 只读 | 只读 |
| **入退日期** | ✅ 可编辑 | ✅ 可编辑 | ✅ 可编辑 | ✅ 可编辑 |
| **原因** | ✅ 可编辑 | ✅ 可编辑 | ✅ 可编辑 | ✅ 可编辑 |
| **备注** | ✅ 可编辑 | ✅ 可编辑 | ✅ 可编辑 | ✅ 可编辑 |
| 登记日期 | 只读（灰底） | 只读 | 只读 | 只读 |
| 登记人 | 只读（灰底） | 只读 | 只读 | 只读 |

### 13.5 校验联动（编辑提交时执行）

| # | 校验项 | 触发条件 | 校验规则 | 错误提示 |
|---|--------|---------|---------|---------|
| V-EDIT-01 | 入退日期非空 | 必填字段 | `BookingDate IS NOT NULL` | `请选择入退日期` |
| V-EDIT-02 | 入退日期 ≤ 今天 | 任意记录 | `BookingDate ≤ now()` | `入退日期不能晚于今天` |
| V-EDIT-03 | 一进一出顺序 | 状态变为「在宿」或「已退房」 | 取该员工全部 BOOKINGS 按日期排序，交替：入住→退房→入住→退房 | `请检查日期顺序：与上次操作冲突` |
| V-EDIT-04 | 入住日期 ≥ 入职日期 | 类型=1 | `BookingDate ≥ SysEmployee.HireDate` | `入住日期不能早于入职日期 {hireDate}` |
| V-EDIT-05 | 退房日期 ≥ 入住日期 | 类型=2 | `BookingDate ≥ 关联入住记录的 BookingDate` | `退房日期不能早于入住日期 {checkInDate}` |
| V-EDIT-06 | 退房日期 ≤ 下次入住 | 类型=2 | `BookingDate < 下次入住.bookingDate - 1天` | `退房日期不能晚于下次入住 {nextCheckInDate}` |
| V-EDIT-07 | 房间余量 | 入住记录变更宿舍时 | `Dorm.CurrentResidentCount ≤ Dorm.Capacity` | `房间 {dormCode} 余量不足` |
| V-EDIT-08 | 同员工同日期唯一 | 任意编辑 | 排除自身后，`(EmployeeId, BookingDate)` 唯一 | `该员工该日期已存在办理记录` |

### 13.6 关联基础资料字典引用

| 字典名 | 表/常量 | 字段 | 取值 | 在本页使用场景 |
|--------|---------|------|------|---------------|
| `BOOKING_TYPES` | `DormBooking.Type` | TINYINT | 1=入住 2=退房 | 类型字段文案翻译 |
| `BOOKING_STATUSES` | `DormBooking.Status` | TINYINT | 1=预约 2=在宿 3=已退房 4=已取消 | 状态字段文案翻译 + Badge 配色 |
| `BOOKING_STATUS_BADGE` | 同上 | — | bg-warning / bg-success / bg-secondary / bg-danger | 状态 Badge 渲染 |
| `ATTENDANCE_TYPES` | `AttendanceType` | INT PK + Code | 6 种（DEFAULT/MORNING/MIDDLE/EVENING/NIGHT/OTHER） | 考勤班次 Badge 显示（运行时 JOIN） |
| `ATTENDANCE_NAME` | 同上 | — | 默认/早班/中班/晚班/夜班/其他 | 中文名翻译 |
| `ATTENDANCE_BADGE` | 同上 | — | bg-secondary / bg-warning / bg-primary / bg-dark / bg-info | Badge 配色 |
| `DEPARTMENTS` | `Department` | INT PK + Code + Name | 8 个（生产部/技术部/.../其他） | 不直接显示，但通过员工冗余字段 Department 间接引用 |
| `EMPLOYEE_TYPES` | `EmployeeType` | INT PK + Code + Name | 5 种（合同/临时/外包/实习/驻场） | 不在本页显示，但通过员工冗余字段 |
| `EMPLOYMENT_STATUSES` | `EmploymentStatus` | INT PK + Code + Name | 3 种（在职/待入职/已离职） | 不在本页显示，但编辑需校验员工在职状态 |
| Reason 字典 | `DormBooking.Reason` | string | 入职/调岗/换房/预约/离职/其他 | `<select>` 下拉硬编码（前端固定枚举） |

### 13.7 接口契约

**请求**：`GET /Booking/Edit/{id}`

**响应（页面渲染所需数据）**：

```json
{
  "id": 12345,
  "employeeId": 108,
  "employeeCode": "EMP-2026-080",
  "employeeName": "林涛",
  "attendanceType": "MORNING",
  "attendanceTypeName": "早班",
  "attendanceBadge": "bg-warning text-dark",
  "dormCode": "D-088",
  "type": 1,
  "typeName": "入住",
  "status": 2,
  "statusName": "在宿",
  "statusBadge": "bg-success",
  "bookingDate": "2026-02-16",
  "reason": "调岗",
  "remark": "由 D-055 调宿至 D-088",
  "registrationDate": "2026-02-15T09:30:00",
  "registrar": "admin"
}
```

**请求**：`PUT /api/v1/bookings/{id}`

**请求体**：

```json
{
  "bookingDate": "2026-02-16",
  "reason": "调岗",
  "remark": "由 D-055 调宿至 D-088",
  "status": 2
}
```

> ⚠️ `status` 仅当原状态=1（预约）时允许提交，其他状态下前端禁用下拉，提交后端拒绝。
> 其余字段（员工/宿舍/类型/考勤班次/登记日期/登记人）均**不接受 PUT 修改**，由后端忽略或 400 报错。

### 13.8 数据写入流程

```
[编辑页面提交]
    │
    ▼
[API: PUT /api/v1/bookings/{id}]
    │
    ├─► ① 加载 DormBooking 原始记录
    ├─► ② 校验可编辑字段（BookingDate / Reason / Remark / Status?）
    ├─► ③ 触发校验 V-EDIT-01 ~ V-EDIT-08
    │     ├─ 失败：返回 400 + 错误码（INVALID_DATE / DATE_CONFLICT / ...）
    │     └─ 通过：进入 ④
    ├─► ④ 若 Status 从 1 改为 2（在宿）：
    │     ├─ 更新 DormBooking.Status = 2
    │     └─ 联动更新 SysEmployee.DormCode = 该记录 DormCode（冗余同步）
    ├─► ⑤ 若 Status 从 1 改为 3（已退房）：
    │     ├─ 更新 DormBooking.Status = 3
    │     └─ 联动：SysEmployee.DormCode = NULL（释放宿舍）
    ├─► ⑥ 若 Status 从 1 改为 4（已取消）：
    │     └─ 仅更新 DormBooking.Status = 4，不联动 SysEmployee
    ├─► ⑦ 更新 DormBooking.UpdatedAt = now()
    └─► ⑧ 返回 ApiResponse { success: true, data: 更新后的记录 }
```

### 13.9 与其他模块的联动

| 联动模块 | 触发条件 | 联动行为 |
|---------|---------|---------|
| **人员清单** | 状态改为 在宿（2） | `SysEmployee.DormCode = DormBooking.DormCode` |
| **人员清单** | 状态改为 已退房（3） | `SysEmployee.DormCode = NULL` |
| **宿舍档案** | 状态变更 | `Dorm.CurrentResidentCount` 重新查询 DormBooking（Status=2） |
| **宿舍详情** | 任意编辑 | 历史记录列表重新加载，时间顺序可能变化 |
| **首页数据看板** | 任意编辑 | 在宿/退房/预约 KPI 重新计算（异步） |
| **考勤班次** | 不联动 | 考勤班次来源于 SysEmployee，本页只读展示 |

---

## 14. BUGFIX 记录：列表筛选房号条件失效（v2.11.14 修复）

### 14.1 问题描述

**用户反馈**：在办理登记列表的"房号"筛选输入框输入关键字（如"D"），列表返回空结果；但 mock 数据中存在大量房号以"D"开头的记录（如 `D-001`、`D-088`、`D-126` 等），预期应该返回这些记录。

**复现步骤**：
1. 打开 `04-HTML原型/booking/index.html` 办理登记列表
2. 在"房号"输入框输入"D"（或任意房号前缀字符）
3. 观察列表：返回 0 条结果（BUG）

**预期结果**：返回所有房号包含"D"的记录（约 750+ 条）。

### 14.2 根因分析

#### JavaScript 运算符优先级问题

原型 `getFiltered()` 函数原代码：

```javascript
if (dc && !(b.dormCode || "").toLowerCase().indexOf(dc) > -1) return false;
```

**问题分析**：JS 运算符优先级 `!` 高于 `>`，导致表达式解析为：

```javascript
if (dc && (!((b.dormCode || "").toLowerCase().indexOf(dc)) > -1)) return false;
```

逐步演算（以 `b.dormCode = "D-085"`、`dc = "d"` 为例）：

| 步骤 | 表达式 | 结果 |
|------|--------|------|
| 1 | `(b.dormCode || "").toLowerCase()` | `"d-085"` |
| 2 | `.indexOf("d")` | **`0`**（匹配位置 0） |
| 3 | `!0` | **`true`**（逻辑非） |
| 4 | `true > -1` | **`true`**（`true` 强转为 `1`，`1 > -1`） |
| 5 | `dc && true` | **`true`**（条件恒真） |
| 6 | `return false` | **执行**（错误地排除所有记录） |

**关键陷阱**：`indexOf()` 返回 `0` 时（命中位置在字符串开头），`!0 = true`，再加上 `true > -1` 也为 `true`，导致**条件恒真**，所有记录都被排除。

> 同文件中的 `keyword`（员工姓名/工号/手机号）与 `department` 筛选也存在相同 BUG 模式（但因 keyword 字段是用临时变量 `h` 串联 OR 表达式，部分场景下还能"侥幸"工作；`dc`（房号）则**完全失效**）。

### 14.3 修复方案

#### 方案 A：增加显式括号（最小修改）

```javascript
if (dc && ((b.dormCode || "").toLowerCase().indexOf(dc) > -1) === false) return false;
// 或等价写法
if (dc && ((b.dormCode || "").toLowerCase().indexOf(dc)) === -1) return false;
```

#### 方案 B：改用 === -1 判断（推荐，更易读）

```javascript
if (dc && (b.dormCode || "").toLowerCase().indexOf(dc) === -1) return false;
```

**最终采用方案 B**：语义最清晰（"未找到" = `indexOf === -1`），消除运算符优先级歧义。

### 14.4 影响范围

| 文件 | 行号 | 状态 |
|------|------|------|
| `00-方案文档/04-HTML原型/booking/index.html` | 349-350（keyword/department/dormCode 三处） | ✅ 已修复（v2.11.14） |
| `00-方案文档/04-HTML原型/personnel/list.html` | 284-286 | ✅ 无此 BUG（用 `> -1` 直接比较，无 `!`） |
| `00-方案文档/04-HTML原型/dorms/list.html` | 276 | ✅ 无此 BUG（用 `> -1` 直接比较） |
| `DormManage.Admin/Pages/Booking/Index.cshtml` | — | ✅ 无影响（前端不写 JS 筛选逻辑，由后端 C# EF Core LINQ 处理） |
| `DormManage.Shared/Services/BookingService.cs` | line 142 | ✅ 无 BUG（C# `string.Contains(str)` 直接返回 `bool`，无运算符优先级问题） |

### 14.5 验收用例

| 输入 | 期望 | 修复前 | 修复后 |
|------|------|--------|--------|
| `dormCode = "D"` | 返回所有 `D-xxx` 记录 | ❌ 0 条（被全排除） | ✅ ~750+ 条 |
| `dormCode = "085"` | 返回所有 `D-085` 记录 | ❌ 0 条 | ✅ ~5 条 |
| `dormCode = "D-088"` | 返回所有 `D-088` 记录 | ❌ 0 条 | ✅ ~3 条 |
| `dormCode = ""` | 返回所有记录 | ✅ 全部 | ✅ 全部 |
| `dormCode = "XYZ"` | 返回 0 条 | ❌ 0 条 | ✅ 0 条 |

### 14.6 防退化建议

1. **代码规范**：禁止在项目中使用 `!(expr > -1)` / `!(expr >= 0)` 模式，统一改用 `expr === -1` 或 `!== -1`
2. **ESLint 规则**：建议添加 `no-unsafe-negation` 自定义规则检测此模式
3. **单元测试**：为 `getFiltered()` 函数添加针对每个筛选条件的边界用例（特别是 `indexOf = 0` 的场景）

---

## 15. 筛选条件持久化（v2.12.10 新增）

> **规则**：办理登记列表页的所有筛选区字段值自动保存到 localStorage，切换 Tab 页签或刷新页面后自动恢复。

### 15.1 筛选字段列表

| 字段 ID | 标签 | 元素类型 | localStorage Key |
|--------|------|---------|-----------------|
| `keyword` | 员工（姓名/工号/手机） | text input | `dormmanage:filter:v1:booking:admin` |
| `department` | 部门 | text input | 同上 |
| `dormCode` | 房号 | text input | 同上 |
| `type` | 类型 | select | 同上 |
| `status` | 状态 | select | 同上 |
| `dateFrom` | 入退日期从 | date input | 同上 |
| `dateTo` | 到 | date input | 同上 |

### 15.2 行为规则

| 行为 | 规则 |
|------|------|
| **自动保存** | 用户修改任意筛选字段时，自动将当前模块所有筛选值写入 localStorage |
| **自动恢复** | 页面加载时自动从 localStorage 读取当前模块的筛选值，填入对应 DOM 元素 |
| **Tab 切换保留** | 切换 Tab 页签再返回时，筛选值自动恢复 |
| **刷新页面保留** | 刷新浏览器页面时，筛选值自动恢复 |
| **重置清空** | 点击"重置"按钮时，清空 localStorage 中当前模块的筛选值并重置所有筛选字段为空 |

### 15.3 重置按钮实现

```html
<!-- v2.12.10 新增：JS 调用重置，不整页跳转 -->
<button class="btn btn-outline-secondary filter-btn" onclick="FilterPersistence.resetFilters('booking')">
    <i class="bi bi-arrow-clockwise"></i> 重置
</button>
```

---

## 12. 版本历史

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v2.11.5 | 2026-07-12 | 新增 §13 修改办理登记页面字段数据来源与关系矩阵；字段描述与列表表头"办理日期"统一改为"入退日期" |
| v2.11.5.b | 2026-07-12 | 原型 edit.html 数据关联可视化样式规范化：双卡片布局 + info-item 规范 + 图标 tooltip 替代错乱徽章；修复 HTML 嵌套问题；新增 renderSourceHint / renderInfoItem 辅助函数 |
| v2.11.6 | 2026-07-12 | 业务术语统一："预订" → "预约"（含 DormBooking.Status=1 状态文案、状态机、字典定义、Mock 数据说明等）；办理登记列表新增"登记时间"列（来源 DormBooking.RegistrationDate，格式 yyyy-MM-dd HH:mm，位置在登记人与操作之间） |
| v2.11.7 | 2026-07-12 | 列表操作列"编辑"按钮文本更名为"**修改**"；新增权限规则："修改"按钮**仅 Status=1 预约**时可点，其他状态（2/3/4）一律 disabled 锁定（灰色 + 🔒 图标 + 悬停提示"仅预约状态可修改"）；同时确认"退房"仅 Status=2 在宿时显示、"删除"仅 Status=1/4 时显示的既有规则 |
| v2.11.7.b | 2026-07-12 | 只读"修改"按钮**去掉 🔒 图标**，**仅显示"修改"文本**（极简风格：disabled 灰色 + 悬停 title="仅预约状态可修改"）；保留 v2.11.7 的整体权限规则不变 |
| v2.11.8 | 2026-07-12 | **操作列新增"入住"快速确认按钮**：仅当 Type=1 入住 && Status=1 预约时显示绿色 `btn-outline-success` "入住"按钮；点击调用新接口 `POST /api/v1/bookings/{id}/confirm-checkin`，服务端将 Status 即时变更为 2 在宿，并同步 Registrar=当前登录账号、RegistrationDate=now()、UpdatedAt=now()，联动更新 SysEmployee.DormCode；新增 §4.1 接口契约；§6.3 操作列定义扩展为"入住 / 修改 / 退房 / 删除"四键位 |
| v2.11.8.b | 2026-07-12 | 入住按钮的房间床位余量校验文案优化：当 `Dorm.CurrentResidentCount >= Dorm.Capacity` 时，前端提示由原"房间 {dormCode} 余量不足"优化为更友好的 **"床位已满，请更换其他房间"**；明确"有余量 → 状态变更为 在宿"与"无余量 → 提示用户更换房间"两条分支路径；保留 V-CONFIRM-04 校验逻辑不变 |
| v2.11.9 | 2026-07-12 | 操作列按钮位置调整：将"入住"按钮位置由原"修改"**左侧**调整为"修改"**右侧**；最终操作列从左至右顺序：**修改 → 入住 → 退房**（删除按钮在弹窗或独立实现，不在此列渲染）；保留所有按钮的显示规则与权限不变 |
| v2.11.10 | 2026-07-12 | **操作列新增"撤销"按钮（撤销退房）**：当记录的入退日期 = 服务端当天日期（`BookingDate == today()`）且 Status = 3 已退房时显示青色 `btn-outline-info` "撤销"按钮，位置在"修改"按钮**右侧**；点击调用新接口 `POST /api/v1/bookings/{id}/undo-checkout`，服务端先校验房间床位余量——**有余量**则 Status 即时变更为 2 在宿并同步 Registrar/RegistrationDate/UpdatedAt，联动恢复 SysEmployee.DormCode；**无余量**则返回 400 NO_CAPACITY 提示"**床位已满，撤销退房失败！**"且不修改任何字段；新增 §4.2 接口契约；§6.3 操作列定义扩展为"修改 / 撤销 / 入住 / 退房 / 删除"五键位 |
| v2.11.11 | 2026-07-12 | **操作列最右侧新增"撤销"按钮（撤销预约）**：当 Status = 1 预约时显示红色 `btn-outline-danger` "撤销"按钮，位置在操作列**最右侧**；点击调用新接口 `POST /api/v1/bookings/{id}/cancel-reservation`，服务端将 Status 即时变更为 4 已取消，并同步 `Registrar=当前登录账号`、`RegistrationDate=now()`、`UpdatedAt=now()`（**无需校验房间余量**，因为从未入住）；新增 §4.3 接口契约；§6.3 操作列定义扩展为"修改 / 撤销退房 / 入住 / 退房 / 撤销预约"五键位（两个"撤销"按钮在不同状态下互斥显示） |
| v2.11.12 | 2026-07-12 | **操作列"退房"按钮显示条件扩展**：当 Type=2 退房 && Status=1 预约时，**在"修改"与"撤销预约"之间**显示黄色 `btn-outline-warning` "退房"按钮（一键完成预约退房）；点击调用新接口 `POST /api/v1/bookings/{id}/confirm-checkout`，服务端将 Status 即时变更为 3 已退房，并同步 `Registrar=当前登录账号`、`RegistrationDate=now()`、`UpdatedAt=now()`，联动 `SysEmployee.DormCode=NULL`（员工变为未分配宿舍）；保留 v2.11.7 中 Status=2 在宿时显示退房按钮的既有规则（v2.11.17 起行为变更：Type=1 入住 && Status=2 在宿 → 弹出确认创建退房新记录）；新增 §4.4 接口契约；§6.3 操作列定义扩展为"修改 / 撤销退房(v2.11.10) / 入住(v2.11.8) / 退房(v2.11.12) / 撤销预约(v2.11.11)"五键位 |
| v2.11.13 | 2026-07-12 | **操作按钮点击后即时刷新规则**：点击"退房"/"入住"/"撤销"任一按钮，服务端处理成功后前端**必须即时刷新列表**显示当前记录的最新数据（状态徽章/登记人/登记时间/操作按钮按新状态重新计算）；**Razor 实现**采用 `location.reload()` 整页刷新（简单可靠）；**原型实现**采用 `registerList.reset()` 重拉数据；**错误处理**：失败时不刷新，仅 `alert(result.message)`；新增 §6.3 v2.11.13 业务规则段 |
| v2.11.14 | 2026-07-12 | **BUGFIX：列表筛选房号条件失效修复**——原型 `booking/index.html` 的 `getFiltered()` 函数存在 JS 运算符优先级 BUG：`!(a > -1)` 被解析为 `(!a) > -1`，当 `indexOf` 返回 0（命中首字符）时 `!0=true` 且 `true > -1=true` 导致条件恒真，所有记录被错误排除；修复方案：改用 `(b.dormCode || "").toLowerCase().indexOf(dc) === -1` 显式"未找到"判断；同步修复 `keyword` / `department` 字段相同 BUG；新增 §14 BUGFIX 章节含根因分析 + 验收用例 + 防退化建议 |
| v2.11.15.d | 2026-07-12 | **修改办理登记页面重构**——移除数据关联可视化区（ER 图/字段矩阵/字典/校验/写入流程），改为参照"编辑人员"页面的标准表单布局：顶部只读信息摘要卡片 + 标准表单控件（入退日期/原因/状态/备注） + 系统自动记录区（只读灰底） + 操作按钮；状态仅 Status=1 预约时显示状态下拉；备注增加字数统计（500 字上限） |
| **v2.11.16** | **2026-07-12** | **办理入住/办理退房改为独占弹窗模式（Bootstrap Modal）**——列表页"办理入住"和"办理退房"按钮从独立页面跳转改为 Modal 弹窗，弹窗内包含完整的多步骤表单（员工搜索 → 日期/原因 → 房间选择/在宿记录选择 → 提交），右侧显示校验提示和员工历史；**修改办理登记页面增加员工基本信息只读展示区**——Edit 页面新增 `.employee-info-card` 卡片，展示工号/姓名/部门/员工类型/考勤班次/手机/入职日期/在职状态（从 SysEmployee JOIN 查询），数据值与记录对应项一致（非空值初始状态）；登记人显示为当前用户 |
| **v2.12.34** | **2026-07-13** | **办理入住弹窗全面升级**——① **校验信息移至"操作类型"右侧同行**：区块 3 采用 `d-flex` 同行两栏布局，左栏操作类型固定 100px，右栏校验信息 flex:1；② **校验失败锁定**：员工当前已在宿时，禁用房间下拉 + 禁用提交按钮（`lockActionIn(true)`）；③ **入住日期 > 当前日期时操作类型二选一**：入住日期选择未来日期时，操作类型区域显示"入住"/"预约"两个 radio 按钮（2 选 1）；入住日期 ≤ 当前日期时仅显示"入住"Badge；④ **预分配房号**：校验通过后按优先级自动推荐房间（1.同班次 → 2.入住率高+余量 → 3.同部门），推荐的同时自动选中下拉框并显示床位号；⑤ 校验信息格式：失败时房号加大红色字体加粗（`text-danger fw-bold fs-5`），通过时显示绿色对勾；⑥ 新增 §7.1 入住校验（选择员工后即刻执行）含预分配房号规则表 + 床位号计算；⑦ 新增 §6.1 办理入住交互流程（弹窗布局 ASCII + 5 步流程） |
| **v2.12.35** | **2026-07-13** | **床位号容量变更约束规则**——① 容量减少时已入住人数 ≥ 新容量 → 禁止保存，提示"**该宿舍当前有 X 人在宿，无法将容量减少至 Y 以下**"；② 容量减少时超床位号员工自动从空闲床位中随机重新分配；③ 容量增加时不影响原床位号，仅追加新床位号；④ 床位号重新分配算法：空闲床位集合 = [1..新容量] - [已入住员工床位号] → 超量员工从空闲床位随机抽取 |
| v2.11.17 | **2026-07-13** | **操作列"退房"按钮行为变更 + 新增"撤销"按钮（在宿场景）**——① **退房按钮**：Type=1 入住 && Status=2 在宿时，点击后弹出确认对话框（展示当前日期和类型为"退房"），确认后调用新接口 `POST /api/v1/bookings/{id}/confirm-checkout-create`，创建一条 Type=2 退房新记录并将原记录 Status 变更为 3 已退房；② **撤销按钮（在宿）**：Type=1 入住 && Status=2 在宿 && BookingDate==今天时，在"退房"按钮右侧显示红色 `btn-outline-danger` "撤销"按钮，点击调用新接口 `POST /api/v1/bookings/{id}/cancel-today`，将 Status 即时变更为 4 已取消（不创建新记录）；③ 新增 §4.5 撤销在宿接口契约 + §4.6 创建退房记录接口契约；④ 最终操作列按钮顺序：**修改 → 撤销退房(v2.11.10) → 入住(v2.11.8) → 退房(v2.11.17) → 撤销在宿(v2.11.17) → 撤销预约(v2.11.11)**（6 个按钮，在不同状态下互斥显示）；④ 状态机新增 `[在宿] --今日撤销--> [已取消]` 转换 |
| **v2.12.10** | **2026-07-13** | **筛选条件持久化**——① 所有筛选区字段值（keyword/department/dormCode/type/status/dateFrom/dateTo）自动保存到 localStorage；② 切换 Tab 页签 / 刷新页面后自动恢复筛选值；③ 重置按钮改为 `FilterPersistence.resetFilters('booking')` JS 调用，不再整页跳转；④ 新增 §15 筛选条件持久化章节含字段列表 + 行为规则 + 重置按钮实现 |
| v2.11.4 | 2026-07-12 | 增补原型 JavaScript 变量命名规范，避免与 mock-data.js 冲突 |
| v2.11.3 | 2026-07-11 | 完善 Mock 数据完整性（750 条办理登记） |
| v2.11.2 | 2026-07-11 | 数据完整性补充 |
| v2.11 | 2026-07-10 | 初始版本，办理登记替代住宿记录 |
